---
phase: 06-orchestration-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
autonomous: true
requirements: [ORCH-01, ORCH-02]

must_haves:
  truths:
    - "Running the production pipeline with a missing required dependency fails that specific task with an explicit error naming the dependency and install command"
    - "Running the production pipeline with a missing optional dependency prints a warning listing which artifacts will be skipped, then spawns only ready tasks"
    - "RECORD.md is always produced regardless of which production tasks are skipped"
    - "The pre-flight summary table shows task readiness at a glance before any production tasks spawn"
  artifacts:
    - path: "SKILL.md"
      provides: "Pre-flight dependency validation section before production DAG"
      contains: "Pre-flight Dependency Validation"
  key_links:
    - from: "SKILL.md pre-flight section"
      to: "SKILL.md Dependency Pipeline section"
      via: "pre-flight runs before spawning any tasks in the DAG"
      pattern: "Pre-flight.*Dependency Pipeline"
---

<objective>
Add pre-flight dependency validation to the SKILL.md production section so the orchestrator agent checks task dependencies before spawning production tasks.

Purpose: Production tasks currently fail mid-execution when dependencies are missing. Pre-flight validation catches missing dependencies upfront, shows a clear summary table, and spawns only tasks whose dependencies are satisfied -- while always producing the Decision Record regardless.

Output: Updated SKILL.md with a pre-flight validation section inserted before the Dependency Pipeline subsection (~40-60 lines).
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-orchestration-hardening/06-CONTEXT.md
@.planning/phases/06-orchestration-hardening/06-RESEARCH.md

@SKILL.md
</context>

<interfaces>
<!-- Key structure the executor needs from SKILL.md production section -->

SKILL.md production pipeline structure (lines 370-500):
- Line 370: ## Production Pipeline
- Line 371: ### Trigger Logic (table)
- Line 379: ### Session Output Directory
- Line 414: ### Dependency Pipeline (DAG diagram)
- Line 426: ### Production Agents (Tasks A-E descriptions)

The pre-flight section goes AFTER "Session Output Directory" (which ends around line 413)
and BEFORE "Dependency Pipeline" (which starts at line 414).

Production task dependency map (from 06-RESEARCH.md):
| Task | Dependencies | Check Command | Install |
|------|-------------|---------------|---------|
| A (Image Agent) | python3, google-genai, Pillow | `python3 -c "from google import genai; from PIL import Image"` | `pip install google-genai>=1.65.0 Pillow>=10.0.0` |
| B (Presentation) | node, pptxgenjs | `node -e "require('pptxgenjs')"` | `npm install pptxgenjs` |
| C (Document) | node, docx | `node -e "require('docx')"` | `npm install docx` |
| D (Web Page) | none | -- | -- |
| E (Archivist) | python3, weasyprint | `python3 -c "import weasyprint"` | `pip install weasyprint` |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-flight dependency validation section to SKILL.md</name>
  <files>SKILL.md</files>
  <action>
Insert a new "### Pre-flight Dependency Validation" subsection in SKILL.md's Production Pipeline section. It goes AFTER the "Session Output Directory" subsection (ends around line 413) and BEFORE the "Dependency Pipeline" subsection (starts at line 414).

The section must contain:

1. **Preamble paragraph** explaining that before spawning production tasks, the orchestrator validates dependencies for each task using shell commands. State that all production tasks are optional -- the Decision Record (RECORD.md) is always produced regardless of task availability. Per user decision: "Required" means required within a specific task (if Task A can't find google-genai, Task A is skipped with install instructions, but Tasks B-E continue).

2. **Dependency table** with columns: Task | Dependencies | Check Command | Install Command
   Rows:
   - A (Image Agent) | python3, google-genai, Pillow | `python3 -c "from google import genai; from PIL import Image"` | `pip install google-genai>=1.65.0 Pillow>=10.0.0`
   - B (Presentation) | node, pptxgenjs | `node -e "require('pptxgenjs')"` | `npm install pptxgenjs`
   - C (Document) | node, docx | `node -e "require('docx')"` | `npm install docx`
   - D (Web Page) | none | -- | --
   - E (Archivist) | python3, weasyprint | `python3 -c "import weasyprint"` | `pip install weasyprint`

3. **Execution protocol** (numbered steps):
   1. Run all check commands from the table above
   2. Build summary table showing task readiness: Task | Status | Missing Dependencies -- use checkmark for ready tasks, warning marker for skipped tasks with the install command
   3. Print the summary table for user visibility
   4. Spawn ONLY tasks whose dependencies are satisfied
   5. List skipped tasks with their install instructions so the user can enable them for next time
   6. ALWAYS produce RECORD.md regardless of task availability -- the Decision Record is the primary output, production artifacts are supplementary

4. **Note on Task D:** Task D (Web Page) has no external dependencies of its own. It is blocked by Tasks A, B, C in the DAG, but if some of those are skipped, Task D still runs with whatever artifacts are available. Task D should always be spawned.

Do NOT put this section in config/orchestration-protocol.md -- per user decision, pre-flight lives in SKILL.md production section, not in the orchestration protocol.

Do NOT create a Python script for this -- the orchestrator agent performs these checks using its Bash tool. This is agent-readable markdown instructions, not code.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -x -m "not live"</automated>
    Manually verify: grep for "Pre-flight Dependency Validation" in SKILL.md, confirm the section appears before "Dependency Pipeline", confirm table has all 5 tasks, confirm execution protocol has 6 numbered steps.
  </verify>
  <done>SKILL.md contains a Pre-flight Dependency Validation subsection before the Dependency Pipeline. The section has a dependency table covering all 5 production tasks, check commands for each, install instructions for each, and a 6-step execution protocol. The section makes clear that all tasks are optional and RECORD.md is always produced.</done>
</task>

</tasks>

<verification>
- [ ] `grep -c "Pre-flight Dependency Validation" SKILL.md` returns 1
- [ ] The pre-flight section appears before the "Dependency Pipeline" section in SKILL.md
- [ ] The dependency table lists all 5 production tasks (A through E)
- [ ] Each task has a check command and install command (except Task D which has none)
- [ ] The execution protocol states that RECORD.md is always produced
- [ ] The section is markdown instructions for the orchestrator agent, NOT Python code
- [ ] `python3 -m pytest tests/ -m "not live"` passes (no regressions)
</verification>

<success_criteria>
SKILL.md production section has pre-flight validation that enables the orchestrator to check dependencies before spawning tasks, show a summary table of readiness, skip unavailable tasks with install instructions, and always produce the Decision Record.
</success_criteria>

<output>
After completion, create `.planning/phases/06-orchestration-hardening/06-01-SUMMARY.md`
</output>
