---
phase: 11-inline-logging-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/ceo.md
  - agents/c-suite/cao.md
  - agents/c-suite/cco.md
  - agents/c-suite/cfo.md
  - agents/c-suite/ciso.md
  - agents/c-suite/coo.md
  - agents/c-suite/cso.md
  - agents/c-suite/cto.md
  - agents/c-suite/vp-delivery.md
  - agents/c-suite/vp-sales.md
autonomous: true
requirements: [AGINF-02]

must_haves:
  truths:
    - "CEO agent file contains inline logging protocol with config-reading/broadcasting role preserved"
    - "All 8 standard C-suite agent files contain identical inline logging protocol with Write tool method and team lead context passing"
    - "CCO agent file contains inline logging protocol with production-specific terminology (production report, production team lead)"
    - "No CEO or C-suite agent file references config/logging-protocol.md"
  artifacts:
    - path: "agents/ceo.md"
      provides: "CEO with inline logging protocol"
      contains: "No issues = no log file"
    - path: "agents/c-suite/cfo.md"
      provides: "Standard C-suite with inline logging protocol"
      contains: "No issues = no log file"
    - path: "agents/c-suite/cco.md"
      provides: "CCO variant with inline logging protocol"
      contains: "production report"
  key_links:
    - from: "agents/ceo.md"
      to: "inline protocol"
      via: "## Agent Logging section replacement"
      pattern: "No issues = no log file"
    - from: "agents/c-suite/*.md"
      to: "inline protocol"
      via: "## Agent Logging section replacement"
      pattern: "No issues = no log file"
---

<objective>
Replace the file-path logging reference in the CEO agent and all 9 C-suite agent files with inline logging protocol summaries.

Purpose: Make CEO and C-suite agents self-sufficient for logging -- no runtime file read of config/logging-protocol.md needed. Required before Phase 12 dispatch rewrite to avoid double-editing these files.
Output: 10 agent files with inline logging protocol, zero references to config/logging-protocol.md.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-inline-logging-protocol/11-RESEARCH.md
@config/logging-protocol.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace logging reference in CEO agent file</name>
  <files>agents/ceo.md</files>
  <action>
Read `agents/ceo.md`. Find the `## Agent Logging` section (starts at line 343). This section ends at the next `## ` heading (`## Configuration References` at line 351). Replace ONLY the content between the `## Agent Logging` header and `## Configuration References` -- do NOT modify the `## Configuration References` section or anything above `## Agent Logging`.

The current CEO logging text (lines 343-349):
```
## Agent Logging

At the start of each session, read `.cdp-context/config.md` and check the
"Agent Logging" field. If the value is "on", include `LOGGING: ON` and
`SESSION PATH: <absolute-path>` in the Phase 0 broadcast and all downstream
agent prompts. Follow the logging protocol at `config/logging-protocol.md`
for your own error capture.
```

Replace with this exact text (preserving the config-reading/broadcasting role, only replacing the file-path reference with inline protocol):
```
## Agent Logging

At the start of each session, read `.cdp-context/config.md` and check the
"Agent Logging" field. If the value is "on", include `LOGGING: ON` and
`SESSION PATH: <absolute-path>` in the Phase 0 broadcast and all downstream
agent prompts.

For your own error capture, follow this inline protocol:

**When to log:** Only when you encounter tool failures, workarounds applied, data quality issues, instruction ambiguity, or timeout/capacity issues. No issues = no log file.

**File:** `{session-path}/logs/errors-{YYYYMMDD-HHmm}-ceo.md`

**Format:**
```markdown
# Agent Error Log: CEO
**Agent:** ceo  |  **Session:** {session-path}  |  **Date:** {date}
---
## Issue 1: {Brief title}
**What happened:** ...
**Expected:** ...
**Workaround:** ...
**Impact:** ...
```

**Write method:** Use the Write tool to create the log file.

**Rules:** Log as your last action before completing your phase work. If the log write fails, abandon logging and complete your task normally. Logging does not change your analysis or output. Do not mention logging in your output. One tool call max for logging.
```

CRITICAL: The `## Configuration References` section that follows MUST remain intact and unmodified. The blank line between the end of the logging section and `## Configuration References` must be preserved.
  </action>
  <verify>
    <automated>grep -c "logging-protocol.md" agents/ceo.md | grep -q "^0$" && grep -c "No issues = no log file" agents/ceo.md | grep -q "^1$" && grep -c "## Configuration References" agents/ceo.md | grep -q "^1$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CEO agent file has inline logging protocol with config-reading/broadcasting instructions preserved, no reference to config/logging-protocol.md, and Configuration References section intact</done>
</task>

<task type="auto">
  <name>Task 2: Replace logging reference in all 9 C-suite agent files</name>
  <files>agents/c-suite/cao.md, agents/c-suite/cco.md, agents/c-suite/cfo.md, agents/c-suite/ciso.md, agents/c-suite/coo.md, agents/c-suite/cso.md, agents/c-suite/cto.md, agents/c-suite/vp-delivery.md, agents/c-suite/vp-sales.md</files>
  <action>
For each of the 9 C-suite agent files, read the file, find the `## Agent Logging` section, and replace it with the appropriate inline protocol. The section starts at `## Agent Logging` and ends at the next `## ` heading. Do NOT modify anything before or after the logging section.

**For 8 STANDARD C-suite agents** (cao, cfo, ciso, coo, cso, cto, vp-delivery, vp-sales):

Current text pattern (exact text varies only by location in file):
```
## Agent Logging

If agent logging is active for this session (the Phase 0 broadcast or your prompt
contains `LOGGING: ON` and `SESSION PATH:`), follow the error logging protocol at
`config/logging-protocol.md` after completing your synthesis. Pass the logging context
(`LOGGING: ON` and `SESSION PATH:`) to all team lead dispatch prompts.
```

Replace with this IDENTICAL text in all 8 files:
```
## Agent Logging

If agent logging is active for this session (the Phase 0 broadcast or your prompt contains `LOGGING: ON` and `SESSION PATH:`), follow this inline protocol after completing your synthesis. Pass the logging context (`LOGGING: ON` and `SESSION PATH:`) to all team lead dispatch prompts.

**When to log:** Only when you encounter tool failures, workarounds applied, data quality issues, instruction ambiguity, or timeout/capacity issues. No issues = no log file.

**File:** `{session-path}/logs/errors-{YYYYMMDD-HHmm}-{agent-name}.md`

**Format:**
```markdown
# Agent Error Log: {Role Title}
**Agent:** {name}  |  **Session:** {session-path}  |  **Date:** {date}
---
## Issue 1: {Brief title}
**What happened:** ...
**Expected:** ...
**Workaround:** ...
**Impact:** ...
```

**Write method:** Use the Write tool to create the log file.

**Rules:** Log as your last action before SendMessage/TaskUpdate. If the log write fails, abandon logging and complete your task normally. Logging does not change your analysis or output. Do not mention logging in your output or SendMessage. One tool call max for logging.
```

**For CCO** (`agents/c-suite/cco.md`):

Current text:
```
## Agent Logging

If agent logging is active for this session (your prompt contains `LOGGING: ON`
and `SESSION PATH:`), follow the error logging protocol at
`config/logging-protocol.md` after completing your production report. Pass the
logging context (`LOGGING: ON` and `SESSION PATH:`) to all production team lead
dispatch prompts.
```

Replace with this CCO-specific text (note: "production report" not "synthesis", "production team lead" not "team lead", checks "your prompt" not "the Phase 0 broadcast or your prompt"):
```
## Agent Logging

If agent logging is active for this session (your prompt contains `LOGGING: ON` and `SESSION PATH:`), follow this inline protocol after completing your production report. Pass the logging context (`LOGGING: ON` and `SESSION PATH:`) to all production team lead dispatch prompts.

**When to log:** Only when you encounter tool failures, workarounds applied, data quality issues, instruction ambiguity, or timeout/capacity issues. No issues = no log file.

**File:** `{session-path}/logs/errors-{YYYYMMDD-HHmm}-cco.md`

**Format:**
```markdown
# Agent Error Log: CCO
**Agent:** cco  |  **Session:** {session-path}  |  **Date:** {date}
---
## Issue 1: {Brief title}
**What happened:** ...
**Expected:** ...
**Workaround:** ...
**Impact:** ...
```

**Write method:** Use the Write tool to create the log file.

**Rules:** Log as your last action before completing your phase work. If the log write fails, abandon logging and complete your task normally. Logging does not change your analysis or output. Do not mention logging in your output. One tool call max for logging.
```

CRITICAL for all 9 files: The section that follows `## Agent Logging` (varies per file: `## Escalation Brief Capability`, `## Synthesis Instructions`, `## Configuration References`) MUST remain intact and unmodified.

Verify after editing each file that:
1. No reference to `logging-protocol.md` remains
2. The `## Agent Logging` header is present
3. The text "No issues = no log file" is present
4. The section after the logging section still exists unchanged
  </action>
  <verify>
    <automated>count=0; for f in agents/c-suite/*.md; do if grep -q "logging-protocol.md" "$f"; then echo "FAIL: $f still references logging-protocol.md"; exit 1; fi; if grep -q "No issues = no log file" "$f"; then count=$((count+1)); fi; done; if [ "$count" -eq 9 ]; then echo "PASS: all 9 C-suite files updated"; else echo "FAIL: only $count/9 files have inline protocol"; exit 1; fi</automated>
  </verify>
  <done>All 9 C-suite agent files have inline logging protocol (8 standard + 1 CCO variant), no references to config/logging-protocol.md, sections after Agent Logging preserved</done>
</task>

</tasks>

<verification>
```bash
# Verify no CEO/C-suite file references logging-protocol.md
grep -r "logging-protocol.md" agents/ceo.md agents/c-suite/ && echo "FAIL" || echo "PASS: no file-path references"

# Verify all 10 files have inline protocol
grep -rl "No issues = no log file" agents/ceo.md agents/c-suite/ | wc -l
# Expected: 10

# Verify all 10 files have Agent Logging header
grep -rl "## Agent Logging" agents/ceo.md agents/c-suite/ | wc -l
# Expected: 10

# Spot-check CCO uses "production report" not "synthesis"
grep -c "production report" agents/c-suite/cco.md
# Expected: >= 1

# Spot-check CEO preserves config reading
grep -c ".cdp-context/config.md" agents/ceo.md
# Expected: 1
```
</verification>

<success_criteria>
- All 10 files (CEO + 9 C-suite) contain inline logging protocol with "No issues = no log file"
- Zero files reference `config/logging-protocol.md`
- CEO preserves config-reading/broadcasting role
- CCO uses "production report" and "production team lead" terminology
- 8 standard C-suite files have identical logging text
- Sections following `## Agent Logging` in each file are unmodified
</success_criteria>

<output>
After completion, create `.planning/phases/11-inline-logging-protocol/11-01-SUMMARY.md`
</output>
