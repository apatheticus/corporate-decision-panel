---
phase: 11-inline-logging-protocol
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/team-leads/cao/admin-policy-lead.md
  - agents/team-leads/cao/corporate-communications-lead.md
  - agents/team-leads/cao/hr-people-ops-lead.md
  - agents/team-leads/cao/legal-contracts-lead.md
  - agents/team-leads/cco/editor.md
  - agents/team-leads/cco/graphic-designer.md
  - agents/team-leads/cco/publisher.md
  - agents/team-leads/cco/writer.md
  - agents/team-leads/cfo/ap-ar-manager.md
  - agents/team-leads/cfo/controller.md
  - agents/team-leads/cfo/fpa-analyst.md
  - agents/team-leads/cfo/tax-lead.md
  - agents/team-leads/cfo/treasury-manager.md
  - agents/team-leads/ciso/compliance-grc-lead.md
  - agents/team-leads/ciso/identity-access-lead.md
  - agents/team-leads/ciso/security-architecture-lead.md
  - agents/team-leads/ciso/security-operations-lead.md
  - agents/team-leads/coo/facilities-office-manager.md
  - agents/team-leads/coo/operations-manager.md
  - agents/team-leads/coo/process-quality-lead.md
  - agents/team-leads/coo/vendor-procurement-manager.md
  - agents/team-leads/cso/competitive-intelligence-lead.md
  - agents/team-leads/cso/industry-regulatory-analyst.md
  - agents/team-leads/cso/market-intelligence-lead.md
  - agents/team-leads/cso/precedent-patterns-analyst.md
  - agents/team-leads/cso/technology-scout-lead.md
  - agents/team-leads/cto/data-analytics-lead.md
  - agents/team-leads/cto/engineering-lead.md
  - agents/team-leads/cto/infrastructure-devops-lead.md
  - agents/team-leads/cto/product-ux-lead.md
  - agents/team-leads/vp-delivery/client-success-lead.md
  - agents/team-leads/vp-delivery/project-program-manager.md
  - agents/team-leads/vp-delivery/qa-delivery-standards-lead.md
  - agents/team-leads/vp-delivery/resource-manager.md
  - agents/team-leads/vp-sales/account-management-lead.md
  - agents/team-leads/vp-sales/business-development-lead.md
  - agents/team-leads/vp-sales/sales-enablement-lead.md
  - agents/team-leads/vp-sales/sales-operations-lead.md
autonomous: true
requirements: [AGINF-02]

must_haves:
  truths:
    - "All 34 analytical team lead files contain inline logging protocol with Bash heredoc write method"
    - "All 4 CCO production team lead files contain inline logging protocol with Write tool method"
    - "No team lead agent file references config/logging-protocol.md"
    - "Total agent file count remains 48"
  artifacts:
    - path: "agents/team-leads/cfo/controller.md"
      provides: "Representative analytical team lead with inline logging"
      contains: "No issues = no log file"
    - path: "agents/team-leads/cco/graphic-designer.md"
      provides: "Representative CCO production team lead with inline logging"
      contains: "Write tool"
  key_links:
    - from: "agents/team-leads/{cao,cfo,ciso,coo,cso,cto,vp-delivery,vp-sales}/*.md"
      to: "inline protocol"
      via: "3-line reference replaced with inline section"
      pattern: "No issues = no log file"
    - from: "agents/team-leads/cco/*.md"
      to: "inline protocol"
      via: "3-line reference replaced with inline section (Write tool variant)"
      pattern: "No issues = no log file"
---

<objective>
Replace the file-path logging reference in all 38 team lead agent files with inline logging protocol summaries.

Purpose: Make team lead agents self-sufficient for logging -- no runtime file read of config/logging-protocol.md needed. Two variants: analytical team leads (34 files, Bash heredoc) and CCO production team leads (4 files, Write tool).
Output: 38 agent files with inline logging protocol, zero references to config/logging-protocol.md.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-inline-logging-protocol/11-RESEARCH.md
@config/logging-protocol.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace logging reference in 34 analytical team lead files</name>
  <files>agents/team-leads/cao/*.md, agents/team-leads/cfo/*.md, agents/team-leads/ciso/*.md, agents/team-leads/coo/*.md, agents/team-leads/cso/*.md, agents/team-leads/cto/*.md, agents/team-leads/vp-delivery/*.md, agents/team-leads/vp-sales/*.md</files>
  <action>
For each of the 34 analytical team lead files (all team-leads EXCEPT the 4 in `team-leads/cco/`), read the file and find the 3-line logging reference at the end of the file. These lines appear after the `## Team Communication` section and are always the LAST lines in the file.

Current text (identical in all 34 files):
```
If agent logging is active for this session (your prompt contains `LOGGING: ON`
and `SESSION PATH:`), follow the error logging protocol at `config/logging-protocol.md`
before your final SendMessage and TaskUpdate.
```

Replace those 3 lines with this IDENTICAL text in all 34 files:
```
## Agent Logging

If your prompt contains `LOGGING: ON` and `SESSION PATH: <path>`, error logging is active.

**When to log:** Only when you encounter tool failures, workarounds applied, data quality issues, instruction ambiguity, or timeout/capacity issues. No issues = no log file.

**File:** `{session-path}/logs/errors-{YYYYMMDD-HHmm}-{agent-name}.md`

**Format:**
```markdown
# Agent Error Log: {Role Title}
**Agent:** {name}  |  **Session:** {session-path}  |  **Date:** {date}
---
## Issue 1: {Brief title}
**What happened:** ...
**Expected:** ...
**Workaround:** ...
**Impact:** ...
```

**Write method:** Use Bash with a heredoc and single-quoted delimiter (`'LOGEOF'`).

**Rules:** Log as your last action before SendMessage/TaskUpdate. If the log write fails, abandon logging and complete your task normally. Logging does not change your analysis or output. Do not mention logging in your output or SendMessage. One tool call max for logging.
```

Process all 34 files. The files are in these directories (all .md files in each):
- `agents/team-leads/cao/` (4 files)
- `agents/team-leads/cfo/` (5 files)
- `agents/team-leads/ciso/` (4 files)
- `agents/team-leads/coo/` (4 files)
- `agents/team-leads/cso/` (5 files)
- `agents/team-leads/cto/` (4 files)
- `agents/team-leads/vp-delivery/` (4 files)
- `agents/team-leads/vp-sales/` (4 files)

CRITICAL: The `## Team Communication` section and everything above it MUST remain intact. Only the 3-line logging paragraph at the very end of the file is replaced. A `## Agent Logging` header is added to give the new inline section a proper heading.
  </action>
  <verify>
    <automated>count=0; for f in agents/team-leads/{cao,cfo,ciso,coo,cso,cto,vp-delivery,vp-sales}/*.md; do if grep -q "logging-protocol.md" "$f"; then echo "FAIL: $f still references logging-protocol.md"; exit 1; fi; if grep -q "No issues = no log file" "$f"; then count=$((count+1)); fi; done; if [ "$count" -eq 34 ]; then echo "PASS: all 34 analytical team lead files updated"; else echo "FAIL: only $count/34 files have inline protocol"; exit 1; fi</automated>
  </verify>
  <done>All 34 analytical team lead files have inline logging protocol with Bash heredoc write method, no references to config/logging-protocol.md, Team Communication section preserved</done>
</task>

<task type="auto">
  <name>Task 2: Replace logging reference in 4 CCO production team lead files</name>
  <files>agents/team-leads/cco/editor.md, agents/team-leads/cco/graphic-designer.md, agents/team-leads/cco/publisher.md, agents/team-leads/cco/writer.md</files>
  <action>
For each of the 4 CCO production team lead files, read the file and find the 3-line logging reference at the end of the file. These lines appear after the `## Team Communication` section and are always the LAST lines in the file.

Current text (identical in all 4 files):
```
If agent logging is active for this session (your prompt contains `LOGGING: ON`
and `SESSION PATH:`), follow the error logging protocol at `config/logging-protocol.md`
before your final SendMessage and TaskUpdate.
```

Replace those 3 lines with this IDENTICAL text in all 4 files (note: Write tool instead of Bash heredoc -- CCO production team leads use Write tool per the logging protocol):
```
## Agent Logging

If your prompt contains `LOGGING: ON` and `SESSION PATH: <path>`, error logging is active.

**When to log:** Only when you encounter tool failures, workarounds applied, data quality issues, instruction ambiguity, or timeout/capacity issues. No issues = no log file.

**File:** `{session-path}/logs/errors-{YYYYMMDD-HHmm}-{agent-name}.md`

**Format:**
```markdown
# Agent Error Log: {Role Title}
**Agent:** {name}  |  **Session:** {session-path}  |  **Date:** {date}
---
## Issue 1: {Brief title}
**What happened:** ...
**Expected:** ...
**Workaround:** ...
**Impact:** ...
```

**Write method:** Use the Write tool to create the log file.

**Rules:** Log as your last action before SendMessage/TaskUpdate. If the log write fails, abandon logging and complete your task normally. Logging does not change your analysis or output. Do not mention logging in your output or SendMessage. One tool call max for logging.
```

The ONLY difference from the analytical team lead version is the **Write method** line: "Use the Write tool to create the log file" instead of "Use Bash with a heredoc and single-quoted delimiter (`'LOGEOF'`)."

CRITICAL: The `## Team Communication` section and everything above it MUST remain intact.

After editing all 4 files, run the full phase verification suite to confirm all 48 agent files (CEO + C-suite + team leads) are complete.
  </action>
  <verify>
    <automated>count=0; for f in agents/team-leads/cco/*.md; do if grep -q "logging-protocol.md" "$f"; then echo "FAIL: $f still references logging-protocol.md"; exit 1; fi; if grep -q "No issues = no log file" "$f"; then count=$((count+1)); fi; done; if [ "$count" -eq 4 ]; then echo "PASS: all 4 CCO team lead files updated"; else echo "FAIL: only $count/4 files have inline protocol"; exit 1; fi</automated>
  </verify>
  <done>All 4 CCO production team lead files have inline logging protocol with Write tool method, no references to config/logging-protocol.md, Team Communication section preserved</done>
</task>

</tasks>

<verification>
```bash
# FULL PHASE VERIFICATION (run after both plans complete)

# 1. Must return 0 matches -- no file-path references remain
grep -r "logging-protocol.md" agents/
# Expected: no output (exit code 1)

# 2. Must return 48 -- all agent files still exist
find agents/ -name "*.md" -type f | wc -l
# Expected: 48

# 3. Must return 48 -- every agent file has inline logging
grep -rl "No issues = no log file" agents/ | wc -l
# Expected: 48

# 4. Must return 48 -- every agent file has Agent Logging header
grep -rl "## Agent Logging" agents/ | wc -l
# Expected: 48

# 5. Write method correctness
# Analytical team leads should use Bash heredoc
grep -rl "single-quoted delimiter" agents/team-leads/{cao,cfo,ciso,coo,cso,cto,vp-delivery,vp-sales}/ | wc -l
# Expected: 34

# CCO production team leads + C-suite + CEO should use Write tool
grep -rl "Use the Write tool" agents/team-leads/cco/ | wc -l
# Expected: 4
```
</verification>

<success_criteria>
- All 38 team lead files contain inline logging protocol with "No issues = no log file"
- 34 analytical team leads specify Bash heredoc write method
- 4 CCO production team leads specify Write tool method
- Zero files reference `config/logging-protocol.md`
- Total agent file count is 48
- `## Team Communication` sections preserved in all files
</success_criteria>

<output>
After completion, create `.planning/phases/11-inline-logging-protocol/11-02-SUMMARY.md`
</output>
