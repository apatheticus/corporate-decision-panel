---
phase: 12-dispatch-architecture-rewrite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/ceo.md
autonomous: true
requirements: [DISP-05, DISP-08]

must_haves:
  truths:
    - "CEO agent contains TeamCreate instructions for division teams"
    - "CEO agent contains team lead dispatch logic triggered by C-suite SendMessage notifications"
    - "CEO agent contains sub-question file reading before team lead dispatch"
    - "CEO agent contains CSO Phase 1.5 division team sequencing before Phase 2"
    - "CEO agent contains CCO production wave management via SendMessage coordination"
    - "CEO agent contains CSO Phase 2 standalone dispatch simultaneous with other C-suite division teams"
  artifacts:
    - path: "agents/ceo.md"
      provides: "CEO universal dispatcher with division team creation, notification-triggered dispatch, and production wave management"
      contains: "TeamCreate"
  key_links:
    - from: "agents/ceo.md"
      to: "config/orchestration-protocol.md"
      via: "CEO references orchestration protocol for phase sequencing"
      pattern: "orchestration-protocol.md"
    - from: "agents/ceo.md"
      to: "config/dispatch-protocol.md"
      via: "CEO references dispatch protocol for sub-question file convention"
      pattern: "dispatch-protocol.md"
    - from: "agents/ceo.md"
      to: "config/cco-dispatch-protocol.md"
      via: "CEO references CCO dispatch protocol for wave management"
      pattern: "cco-dispatch-protocol.md"
---

<objective>
Rewrite the CEO agent to add universal dispatch capabilities -- TeamCreate for division teams, notification-triggered team lead dispatch, CSO Phase 1.5 sequencing, and CEO-managed production wave coordination.

Purpose: The CEO is the only agent that can use TeamCreate and Agent tools (platform constraint). This plan adds the dispatch mechanics that replace what C-suite agents previously did themselves.

Output: Updated agents/ceo.md with comprehensive dispatch instructions.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dispatch-architecture-rewrite/12-CONTEXT.md
@.planning/phases/12-dispatch-architecture-rewrite/12-RESEARCH.md
@agents/ceo.md

<interfaces>
<!-- The CEO agent references these protocols. Plan 12-01 rewrites them in parallel.
     The executor should write the CEO agent based on the conventions described in
     12-RESEARCH.md and 12-CONTEXT.md (which define the same contracts the protocols
     will contain). The CEO references protocols by path, not inline content. -->

Current CEO agent structure (agents/ceo.md, 382 lines):
- YAML frontmatter (name, description, model)
- Identity and Mandate section
- Orchestration Protocol Reference section (~lines 23-39) -- NEEDS MAJOR UPDATE
- CEO Deliberation (Synthesis) section (~lines 41-172) -- preserve intact
- /evaluate section (~lines 174-231) -- preserve intact
- Multi-Mode Comparison section (~lines 233-291) -- preserve intact
- Susceptibility Mitigation section (~lines 293-311) -- preserve intact
- Tier-Specific Behavior section (~lines 313-341) -- preserve intact
- Agent Logging section (~lines 343-370) -- preserve intact
- Configuration References section (~lines 372-382) -- update references
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CEO Orchestration Protocol Reference section</name>
  <files>agents/ceo.md</files>
  <action>
Read the full `agents/ceo.md` (382 lines). The major change is to the **Orchestration Protocol Reference** section (~lines 23-39). Replace it with a comprehensive dispatch mechanics section. All other sections are preserved intact except where noted below.

**Section: "Orchestration Protocol Reference" -- REWRITE as "Orchestration Protocol Reference" with expanded dispatch mechanics:**

Keep the opening paragraph pointing to `config/orchestration-protocol.md` for the full protocol. Then replace the brief overview bullets with detailed dispatch mechanics:

**Team Architecture (REPLACE existing paragraph):**
Old: "The main session acts as the CEO. C-suite agents are dispatched as standalone background subagents via the Agent tool without team_name."
New: "You (the main session) are the universal dispatcher. You create ALL division teams via TeamCreate and dispatch ALL agents (C-suite and team leads) via the Agent tool. C-suite agents are teammates in your division teams -- they cannot use TeamCreate or Agent tools."

**Phase 1.5 -- CSO Research Division Team (NEW, detailed):**
```
If CSO is activated:
1. mkdir -p {session}/sub-questions/cso
2. TeamCreate("cdp-cso-{slug}")
3. Agent(cso, team_name="cdp-cso-{slug}", prompt=research directive)
4. Wait for CSO SendMessage with sub-question file paths
5. Read each sub-Q file in {session}/sub-questions/cso/
6. Dispatch research team leads as teammates in cdp-cso-{slug}:
   For each sub-Q file, one Agent call with team_name, paste sub-Q content as prompt
7. Wait for CSO to complete (_DOSSIER_cso.md written)
8. Read _DOSSIER_cso.md, incorporate into Phase 0 broadcast
```
CSO Phase 1.5 completes FULLY before Phase 2 begins.

**Phase 0 broadcast:** Include Research Dossier if available (unchanged but re-stated).

**Phase 2 -- Division Team Dispatch (REPLACE existing paragraph):**
```
For each activated analytical role (cfo, cto, coo, ciso, cao, vp-sales, vp-delivery):
  1. mkdir -p {session}/sub-questions/{role}
  2. TeamCreate("cdp-{role}-{slug}")
  3. Agent({role}, team_name="cdp-{role}-{slug}", prompt=CEO framing + Research Dossier if available)

CSO Phase 2 (simultaneous with above):
  Agent(cso, NO team_name, prompt=CEO framing + Research Dossier)
  CSO produces _RECOMMENDATION_cso.md inline without team leads.

Rolling dispatch (notification-triggered, not polling):
  As each C-suite SendMessage arrives:
    "Sub-questions ready: {paths}" -->
      Read each sub-Q file
      Dispatch team leads as teammates in that role's division team
      (one Agent call per team lead, paste sub-Q file content verbatim as prompt,
       do NOT re-summarize or add analytical overlay)
    "No team leads needed -- proceeding with inline analysis" -->
      Note. No further action for that division.
```
Emphasize: CEO is a messenger for dispatch, not a decision-maker about team lead composition. Dispatch exactly the team leads that have sub-question files. Paste sub-Q content verbatim -- preserve C-suite domain translation.

**Context management guidance (NEW, brief):**
"After dispatching team leads for a division, do not retain sub-question file content in working memory. Focus context on monitoring and synthesis."

**Phase 3/4 (update existing bullets):**
Phase 3: Team leads SendMessage findings to C-suite parent (unchanged). CEO does not see team lead outputs.
Phase 4: CEO monitors for _RECOMMENDATION_{role}.md files. Division teams dissolve naturally after recommendation is written. No explicit shutdown needed.

**Phase 4.5 -- Pre-Mortem (PRESERVE existing description):**
Pre-mortem agents dispatched as standalone subagents (no team_name). Prompts include executive summaries only (extracted from _RECOMMENDATION_*.md), not full recommendations. Output: _PREMORTEM_{role}.md. Division teams already dissolved by this point.

**Production -- CEO-Managed CCO Wave Dispatch (REPLACE existing):**
```
1. Write RECORD.md
2. TeamCreate("cdp-cco-{slug}")
3. Agent(cco, team_name="cdp-cco-{slug}", prompt=RECORD content + session context)
4. Wait for CCO SendMessage: "Creative Brief complete, dispatch {agent}"
5. Dispatch wave agent as teammate in cdp-cco-{slug} (Agent with team_name)
   Prompt includes: Creative Brief (from CCO's message or known content),
   RECORD.md content, session context, specification pointer
6. Wait for CCO SendMessage with wave completion and next dispatch instruction
7. Repeat for each wave as CCO directs
8. Revision cycle: If CCO SendMessages revision instructions, re-dispatch
   responsible team lead with instructions. Maximum one revision cycle.
9. Production complete when CCO reports completion.
```
CEO's role in production is PURELY MECHANICAL. Never dispatch a production wave agent without CCO SendMessage authorization.

**Configuration References section (UPDATE):**
Add references to `config/dispatch-protocol.md` (sub-question file convention) and `config/cco-dispatch-protocol.md` (production wave coordination) if not already present.

**PRESERVE ALL OTHER SECTIONS INTACT:**
- Identity and Mandate
- CEO Deliberation (Synthesis) -- Steps 1-5
- /evaluate -- Issue Triage Logic
- Multi-Mode Comparison
- Susceptibility Mitigation
- Tier-Specific Behavior
- Agent Logging
  </action>
  <verify>
    <automated>bash -c 'TC=$(grep -c "TeamCreate" agents/ceo.md); SQ=$(grep -c "sub-questions" agents/ceo.md); SM=$(grep -c "SendMessage" agents/ceo.md); DOS=$(grep -c "_DOSSIER_cso" agents/ceo.md); if [ "$TC" -ge 3 ] && [ "$SQ" -ge 2 ] && [ "$SM" -ge 3 ] && [ "$DOS" -ge 1 ]; then echo "PASS: TeamCreate=$TC sub-questions=$SQ SendMessage=$SM DOSSIER=$DOS"; else echo "FAIL: TeamCreate=$TC sub-questions=$SQ SendMessage=$SM DOSSIER=$DOS"; exit 1; fi'</automated>
  </verify>
  <done>CEO agent contains: (1) TeamCreate instructions for division teams per role, (2) notification-triggered team lead dispatch with sub-question file reading, (3) CSO Phase 1.5 division team with _DOSSIER_cso.md and sequential completion before Phase 2, (4) CSO Phase 2 standalone dispatch simultaneous with other C-suite, (5) CEO-managed CCO production wave dispatch via SendMessage coordination, (6) pre-mortem as standalone subagents. All existing sections (Deliberation, /evaluate, Multi-Mode, Susceptibility, Tier-Specific, Logging) preserved intact.</done>
</task>

</tasks>

<verification>
1. `grep -c "TeamCreate" agents/ceo.md` returns 3+ (CSO team, division teams, production team)
2. `grep -c "sub-questions" agents/ceo.md` returns 2+ (directory creation, file reading)
3. `grep "standalone" agents/ceo.md` returns matches ONLY in pre-mortem and CSO Phase 2 contexts
4. `grep "_DOSSIER_cso" agents/ceo.md` returns at least 1 match
5. `grep "SendMessage" agents/ceo.md` returns 3+ matches (C-suite notification, CCO wave coordination)
6. Full read-through confirms all non-dispatch sections preserved intact
</verification>

<success_criteria>
- CEO agent has TeamCreate instructions for division team creation (DISP-05)
- CEO agent has team lead dispatch triggered by C-suite SendMessage notifications with sub-question file reading (DISP-05)
- CEO agent has CSO Phase 1.5 division team dispatch with sequential completion before Phase 2 (DISP-08)
- CEO agent has CSO Phase 2 standalone dispatch simultaneous with other C-suite (DISP-08)
- CEO agent has CCO production wave management via SendMessage (DISP-05)
- Pre-mortem dispatch preserved as standalone subagents (DISP-05)
- All existing non-dispatch sections preserved verbatim
</success_criteria>

<output>
After completion, create `.planning/phases/12-dispatch-architecture-rewrite/12-02-SUMMARY.md`
</output>
