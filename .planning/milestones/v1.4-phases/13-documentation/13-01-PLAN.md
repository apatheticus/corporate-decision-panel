---
phase: 13-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/orchestration-protocol.md
  - agents/ceo.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "Orchestration protocol Phase 4 section contains guidance on reading large recommendation files with offset/limit parameters"
    - "CEO agent Step 1 contains guidance on handling truncated recommendation files with offset/limit parameters"
  artifacts:
    - path: "config/orchestration-protocol.md"
      provides: "Large file read guidance in Phase 4 synthesis section"
      contains: "offset"
    - path: "agents/ceo.md"
      provides: "Large file read guidance in CEO Deliberation Step 1"
      contains: "offset"
  key_links:
    - from: "config/orchestration-protocol.md"
      to: "agents/ceo.md"
      via: "Both reference Read tool offset/limit for _RECOMMENDATION_{role}.md files"
      pattern: "offset.*limit|Large.*Recommendation|Large.*file"
---

<objective>
Add large file read guidance to the orchestration protocol and CEO agent definition.

Purpose: A production session on 2026-03-08 revealed that the CAO's 54.6KB recommendation file was truncated by the Read tool's 2000-line default. While the summary-first synthesis approach naturally mitigates this (executive summaries are in the first 50 lines), the CEO agent needs explicit guidance on using the Read tool's `offset` and `limit` parameters for deep-dive reads of large recommendation files.

Output: Two small markdown insertions -- one in `config/orchestration-protocol.md` Phase 4, one in `agents/ceo.md` Step 1.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-documentation/13-RESEARCH.md

@config/orchestration-protocol.md
@agents/ceo.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add large file read guidance to orchestration protocol Phase 4</name>
  <files>config/orchestration-protocol.md</files>
  <action>
Insert a new subsection after the "### Synchronization" subsection in Phase 4 (after the paragraph ending with "a missing recommendation is not a blocker, it is an acknowledged gap." and BEFORE the `---` separator that precedes Phase 4.5).

Search for the anchor text "a missing recommendation is not a blocker" to find the exact insertion point. Insert the following markdown block immediately after that paragraph:

```markdown

### Large Recommendation Files

Recommendation files from complex deliberations may exceed the Read tool's 2000-line default. When reading `_RECOMMENDATION_{role}.md` files, if a file appears truncated, re-read with `offset` and `limit` parameters to retrieve remaining sections. Executive summaries are always in the first 50 lines -- the summary-first synthesis approach (Step 1 of CEO Deliberation) naturally handles most cases even if the file is truncated.
```

Do NOT modify any other content. The insertion goes between the Synchronization paragraph and the `---` separator.
  </action>
  <verify>
    <automated>grep -n "Large Recommendation Files" config/orchestration-protocol.md && grep -c "offset" config/orchestration-protocol.md</automated>
  </verify>
  <done>config/orchestration-protocol.md Phase 4 section contains a "Large Recommendation Files" subsection with guidance on using Read tool offset/limit parameters. The subsection appears between the Synchronization content and the Phase 4.5 separator.</done>
</task>

<task type="auto">
  <name>Task 2: Add large file read guidance to CEO agent Step 1</name>
  <files>agents/ceo.md</files>
  <action>
Insert a new paragraph into "#### Step 1: Read Executive Summaries and Detect Conflicts" in the "## CEO Deliberation (Synthesis)" section. The insertion goes AFTER the "**Audit trail:**" paragraph (which ends with 'or "None -- no conflicts detected."') and BEFORE "#### Step 2: Fault-Line Analysis".

Search for the anchor text "None -- no conflicts detected" to find the exact insertion point. Insert the following markdown block immediately after that paragraph:

```markdown

**Large files:** If a `_RECOMMENDATION_{role}.md` file exceeds the Read tool's 2000-line default, it will be truncated. Executive summaries are in the first 50 lines, so summary-first synthesis works regardless. If you need the full recommendation (conflict-triggered deep-dive), re-read with `offset` and `limit` parameters to retrieve remaining content.
```

Do NOT modify any other content. The insertion goes between the Audit trail paragraph and Step 2.
  </action>
  <verify>
    <automated>grep -n "Large files:" agents/ceo.md && grep -c "offset" agents/ceo.md</automated>
  </verify>
  <done>agents/ceo.md Step 1 of CEO Deliberation contains a "Large files:" guidance paragraph with instructions on using Read tool offset/limit parameters for truncated recommendation files. The paragraph appears after the audit trail note and before Step 2.</done>
</task>

</tasks>

<verification>
Both files contain large file read guidance:
```bash
grep -n "offset.*limit\|limit.*offset\|Large.*File\|Large.*Recommendation" config/orchestration-protocol.md agents/ceo.md
```

Orchestration protocol guidance is in Phase 4 section (between Synchronization and Phase 4.5):
```bash
grep -A2 "Large Recommendation" config/orchestration-protocol.md
```

CEO agent guidance is in Step 1 of CEO Deliberation (between audit trail and Step 2):
```bash
grep -A2 "Large files:" agents/ceo.md
```
</verification>

<success_criteria>
- `grep -c "offset" config/orchestration-protocol.md` returns at least 1
- `grep -c "offset" agents/ceo.md` returns at least 1
- The guidance text appears in the correct section of each file (Phase 4 for orchestration-protocol.md, Step 1 for ceo.md)
- No other content in either file is modified
</success_criteria>

<output>
After completion, create `.planning/phases/13-documentation/13-01-SUMMARY.md`
</output>
