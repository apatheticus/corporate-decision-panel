---
phase: 09-integration-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/panel-assessment.md
  - SKILL.md
autonomous: true
requirements:
  - TEST-01
  - SPEC-03
  - ORCH-05

must_haves:
  truths:
    - "panel-assessment.md Escalation Note section contains a Triggered Thresholds field with per-condition enumeration format"
    - "Triggered Thresholds format matches orchestration-protocol.md Step 5 (N. [Condition Name] -- TRIGGERED: [reasoning])"
    - "SKILL.md frontmatter invocation list includes /cdp:cleanup as the 6th entry"
    - "SKILL.md Invocation Grammar section contains a Session Cleanup subsection after Production Re-run"
  artifacts:
    - path: "templates/panel-assessment.md"
      provides: "Triggered Thresholds field in Escalation Note section"
      contains: "Triggered Thresholds:"
    - path: "SKILL.md"
      provides: "/cdp:cleanup discoverability in frontmatter and Invocation Grammar"
      contains: "/cdp:cleanup"
  key_links:
    - from: "templates/panel-assessment.md"
      to: "config/orchestration-protocol.md"
      via: "Matching threshold enumeration format (Step 5 per-condition format)"
      pattern: "Triggered Thresholds:.*TRIGGERED:"
    - from: "templates/panel-assessment.md"
      to: "test-scenarios/tier2-partial-activation.md"
      via: "Expected Output Excerpt references threshold enumeration in Escalation Note"
      pattern: "Triggered Thresholds:"
    - from: "SKILL.md"
      to: "commands/cdp/cleanup.md"
      via: "Invocation Grammar entry summarizes cleanup command behavior"
      pattern: "/cdp:cleanup"
---

<objective>
Close the two non-blocking integration gaps (INT-01 and INT-02) identified by the v1.1 milestone audit.

Purpose: The panel-assessment.md template is missing a structured field for threshold-driven escalation enumeration (INT-01), and the /cdp:cleanup command is undiscoverable from SKILL.md (INT-02). Both gaps break the wiring between specifications and their consumer documents.

Output: Updated templates/panel-assessment.md with Triggered Thresholds field and updated SKILL.md with /cdp:cleanup in frontmatter and Invocation Grammar.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integration-polish/09-RESEARCH.md

@templates/panel-assessment.md
@SKILL.md
@config/orchestration-protocol.md
@test-scenarios/tier2-partial-activation.md
@commands/cdp/cleanup.md

<interfaces>
<!-- Key format contracts the executor needs for INT-01. -->

From config/orchestration-protocol.md Step 5 (lines 102-108) -- the per-condition threshold format:
```
- **Threshold Assessment:**
  1. Irreversibility: [TRIGGERED/NOT TRIGGERED] -- [one-sentence reasoning citing diagnostic question result]
  2. Headcount Impact: [TRIGGERED/NOT TRIGGERED] -- [one-sentence reasoning]
  3. Market Position Change: [TRIGGERED/NOT TRIGGERED] -- [one-sentence reasoning]
  4. Existential Financial Risk: [TRIGGERED/NOT TRIGGERED] -- [one-sentence reasoning]
  5. Domain Uncertainty: [TRIGGERED/NOT TRIGGERED] -- [one-sentence reasoning]
  Full activation: [YES (conditions N triggered) / NO]
```

From test-scenarios/tier2-partial-activation.md Expected Output Excerpt (lines 96-127) -- how the template field should render:
```
  1. Irreversibility -- TRIGGERED: Acquisition cannot be meaningfully reversed. [...]
  2. Market Position Change -- TRIGGERED: Acquiring a direct competitor [...]
  3. Existential Financial Risk -- TRIGGERED: $45M capital commitment [...]
```

From SKILL.md frontmatter (lines 13-17) -- current invocation list to extend:
```yaml
invocation:
  - /cdp:consult
  - /cdp:panel
  - /cdp:deliberate
  - /cdp:evaluate
  - /cdp:production
```

From commands/cdp/cleanup.md -- syntax and behavior to summarize:
```
/cdp:cleanup [--older-than days?]
```
Default threshold: 30 days. Behavior: discover sessions, filter by age, display confirmation table, request yes/no, delete on confirmation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Triggered Thresholds field to panel-assessment.md Escalation Note (INT-01)</name>
  <files>templates/panel-assessment.md</files>
  <action>
Edit the Escalation Note section of `templates/panel-assessment.md` (lines 105-116). Insert a `Triggered Thresholds:` field between `Escalation Rationale:` and `Additional Domains for Tier 3:`.

The new field must use this exact format (matching orchestration-protocol.md Step 5 per-condition format and the TEST-01 expected output):

```
Triggered Thresholds: [List each triggered full-activation threshold
  condition with its status and reasoning. Format per condition:
  N. [Condition Name] -- TRIGGERED: [one-sentence reasoning]
  Include only TRIGGERED conditions. If no thresholds were triggered,
  omit this field.]
```

Important constraints:
- The Escalation Note section MUST remain marked as `[Optional]` -- do not change the optional nature.
- Insert AFTER `Escalation Rationale:` and BEFORE `Additional Domains for Tier 3:` -- do not reorder existing fields.
- The per-condition format (`N. [Condition Name] -- TRIGGERED: [reasoning]`) must match both config/orchestration-protocol.md Step 5 (lines 103-108) and test-scenarios/tier2-partial-activation.md Expected Output Excerpt (lines 104-116).
- Do not modify any other section of the file.
  </action>
  <verify>
    <automated>grep -c "Triggered Thresholds:" templates/panel-assessment.md | grep -q "1" && grep -A 4 "Triggered Thresholds:" templates/panel-assessment.md | grep -q "TRIGGERED:" && echo "PASS: Triggered Thresholds field present with correct format" || echo "FAIL"</automated>
  </verify>
  <done>templates/panel-assessment.md Escalation Note section contains a Triggered Thresholds field positioned between Escalation Rationale and Additional Domains for Tier 3, using the per-condition enumeration format (N. [Condition Name] -- TRIGGERED: [reasoning]) that matches both orchestration-protocol.md Step 5 and the TEST-01 expected output.</done>
</task>

<task type="auto">
  <name>Task 2: Add /cdp:cleanup to SKILL.md frontmatter and Invocation Grammar (INT-02)</name>
  <files>SKILL.md</files>
  <action>
Make two edits to `SKILL.md`:

**Edit 1 -- Frontmatter:** Add `/cdp:cleanup` as the last entry in the `invocation:` list (after `/cdp:production`). The result should be:

```yaml
invocation:
  - /cdp:consult
  - /cdp:panel
  - /cdp:deliberate
  - /cdp:evaluate
  - /cdp:production
  - /cdp:cleanup
```

**Edit 2 -- Invocation Grammar body:** Add a new `### Session Cleanup` subsection after the Production Re-run subsection (after line 169, before the `---` separator that precedes the Decision Modes section). The entry must follow the same structure as other Invocation Grammar entries (code block with syntax, brief description, examples). Use this content:

```markdown
### Session Cleanup
```
/cdp:cleanup [--older-than days?]
```
Deletes old CDP session directories from `.cdp-output/` with age-based
filtering and a confirmation prompt before deletion. Default threshold
is 30 days.

- `/cdp:cleanup` -- delete sessions older than 30 days
- `/cdp:cleanup --older-than 7` -- delete sessions older than 7 days
```

Important constraints:
- Maintain exact indentation and dash-prefix format in the frontmatter YAML list.
- Place the Session Cleanup subsection AFTER Production Re-run and BEFORE the `---` separator that introduces the Decision Modes section.
- Match the conciseness level of other Invocation Grammar entries (3-8 lines of description plus examples). Do not reproduce the full cleanup command protocol from commands/cdp/cleanup.md.
- Do not modify any other section of the file.
  </action>
  <verify>
    <automated>grep -c "/cdp:cleanup" SKILL.md | grep -q "[2-9]" && grep -A 1 "### Session Cleanup" SKILL.md | grep -q "cdp:cleanup" && echo "PASS: /cdp:cleanup in frontmatter and Invocation Grammar" || echo "FAIL"</automated>
  </verify>
  <done>SKILL.md frontmatter invocation list contains /cdp:cleanup as the 6th entry, and the Invocation Grammar section contains a Session Cleanup subsection (after Production Re-run, before Decision Modes) with syntax, description, and examples matching the conciseness of other entries.</done>
</task>

</tasks>

<verification>
Cross-reference verification after both tasks complete:

1. **Format consistency (INT-01):** The `Triggered Thresholds:` per-condition format in `templates/panel-assessment.md` matches `config/orchestration-protocol.md` Step 5 format (lines 103-108: `N. [Condition Name] -- TRIGGERED: [reasoning]`)
2. **Test scenario alignment (INT-01):** The template field format is consistent with `test-scenarios/tier2-partial-activation.md` Expected Output Excerpt (lines 104-116)
3. **Frontmatter validity (INT-02):** SKILL.md frontmatter YAML parses correctly with 6 invocation entries
4. **Placement (INT-02):** Session Cleanup subsection appears after Production Re-run and before Decision Modes section
5. **No regressions:** No other sections of either file were modified
</verification>

<success_criteria>
- templates/panel-assessment.md contains Triggered Thresholds field in Escalation Note section with per-condition enumeration format
- SKILL.md frontmatter lists 6 invocation entries including /cdp:cleanup
- SKILL.md Invocation Grammar has Session Cleanup subsection with syntax, description, and examples
- Both changes are pure additions -- no existing content modified or reordered
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-polish/09-01-SUMMARY.md`
</output>
