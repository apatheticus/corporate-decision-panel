---
phase: 07-specification-formalization
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - config/orchestration-protocol.md
  - agents/ceo.md
autonomous: true
requirements:
  - SPEC-03

must_haves:
  truths:
    - "Orchestration protocol Phase 1 Step 4 instructs the CEO to evaluate each threshold condition using diagnostic questions from routing-table.md"
    - "Orchestration protocol Phase 1 Step 5 output format requires structured per-condition TRIGGERED/NOT TRIGGERED evaluation with one-sentence reasoning"
    - "CEO Decision Record template references the structured threshold evaluation format"
    - "CEO.md remains at or under 350 lines"
  artifacts:
    - path: "config/orchestration-protocol.md"
      provides: "Updated Phase 1 Step 4/5 with structured per-condition threshold evaluation format"
      contains: "TRIGGERED/NOT TRIGGERED"
    - path: "agents/ceo.md"
      provides: "Updated Decision Record template referencing structured threshold evaluation"
      contains: "Threshold Assessment"
  key_links:
    - from: "config/orchestration-protocol.md"
      to: "config/routing-table.md"
      via: "Step 4 references routing-table.md for structured decision trees"
      pattern: "routing-table.md"
    - from: "agents/ceo.md"
      to: "config/orchestration-protocol.md"
      via: "Decision Record template references orchestration protocol Step 5 format"
      pattern: "orchestration-protocol.md"
---

<objective>
Update the orchestration protocol's Phase 1 output format to require structured per-condition threshold evaluation, and update the CEO Decision Record template to reference this format.

Purpose: Makes threshold evaluation auditable -- instead of a summary statement ("conditions X and Y triggered"), the CEO must evaluate each condition individually with TRIGGERED/NOT TRIGGERED status and reasoning.
Output: Updated config/orchestration-protocol.md (~325 lines) and agents/ceo.md (still <=350 lines)
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-specification-formalization/07-RESEARCH.md
@.planning/phases/07-specification-formalization/07-01-SUMMARY.md

@config/orchestration-protocol.md
@agents/ceo.md
@config/routing-table.md

<interfaces>
<!-- From config/orchestration-protocol.md Phase 1 (current state, to be modified): -->

Step 4 (lines 80-90): Lists 5 threshold conditions inline with one-sentence descriptions.
Step 5 (lines 92-100): Output format includes "Threshold Conditions: Which of the five full-activation conditions were assessed and their status (triggered / not triggered)"

<!-- From agents/ceo.md Decision Record template (current state, to be modified): -->

Line 120: `Threshold Conditions: [which conditions assessed, which triggered]`

<!-- CEO.md is currently 348/350 lines. Any change must be net-zero or near-zero lines. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update orchestration protocol Phase 1 Step 4 and Step 5 for structured threshold evaluation</name>
  <files>config/orchestration-protocol.md</files>
  <action>
Make two targeted changes to config/orchestration-protocol.md Phase 1:

**Step 4 (lines 80-90) -- Replace inline threshold descriptions with reference to routing-table.md:**

Replace the current 5 numbered threshold conditions (lines 84-88, which have one-sentence descriptions) with:
1. Keep the 5 threshold NAMES listed (Irreversibility, Headcount Impact, Market Position Change, Existential Financial Risk, Domain Uncertainty)
2. Remove the inline one-sentence descriptions (these are now redundant with the expanded decision trees in routing-table.md)
3. Add a reference: "For each condition, use the structured diagnostic questions in `config/routing-table.md` to evaluate whether the threshold is triggered."
4. Keep the existing instruction on line 90: "State threshold assessment explicitly."

Target Step 4:
```
### Step 4: Assess Full-Activation Threshold Conditions

After selecting default routing, assess whether ANY of the following five threshold conditions apply. If **any single condition** is met, **all C-suite members activate** regardless of decision type:

1. **Irreversibility**
2. **Headcount Impact**
3. **Market Position Change**
4. **Existential Financial Risk**
5. **Domain Uncertainty**

For each condition, evaluate using the structured diagnostic questions in `config/routing-table.md`. If ANY diagnostic question for a condition answers YES, that condition is triggered.

State threshold assessment explicitly using the per-condition format below (Step 5).
```

**Step 5 (lines 92-100) -- Replace summary Threshold Conditions line with structured per-condition format:**

Replace the current line:
```
- **Threshold Conditions:** Which of the five full-activation conditions were assessed and their status (triggered / not triggered)
```

With a structured per-condition evaluation format:
```
- **Threshold Assessment:**
  1. Irreversibility: [TRIGGERED/NOT TRIGGERED] -- [one-sentence reasoning citing diagnostic question result]
  2. Headcount Impact: [TRIGGERED/NOT TRIGGERED] -- [one-sentence reasoning]
  3. Market Position Change: [TRIGGERED/NOT TRIGGERED] -- [one-sentence reasoning]
  4. Existential Financial Risk: [TRIGGERED/NOT TRIGGERED] -- [one-sentence reasoning]
  5. Domain Uncertainty: [TRIGGERED/NOT TRIGGERED] -- [one-sentence reasoning]
  Full activation: [YES (conditions N triggered) / NO]
```

Keep all other Step 5 output items (Activated Teams, Excluded Teams, CSO Activation, Override Notes) UNCHANGED.

Estimated net change: approximately +10-15 lines (from 312 to ~325). Do not modify any other sections of the orchestration protocol.
  </action>
  <verify>
    <automated>grep -c "TRIGGERED/NOT TRIGGERED" config/orchestration-protocol.md | xargs test 5 -eq</automated>
  </verify>
  <done>Phase 1 Step 4 lists 5 threshold names with reference to routing-table.md diagnostic questions. Phase 1 Step 5 requires structured per-condition TRIGGERED/NOT TRIGGERED evaluation with reasoning for each condition. All other orchestration protocol sections unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update CEO Decision Record template with structured threshold assessment reference</name>
  <files>agents/ceo.md</files>
  <action>
Make a single targeted change to agents/ceo.md:

Replace line 120:
```
   Threshold Conditions: [which conditions assessed, which triggered]
```

With:
```
   Threshold Assessment: [per-condition TRIGGERED/NOT TRIGGERED with reasoning, per config/orchestration-protocol.md Phase 1 Step 5]
```

This is a same-line text replacement -- zero net new lines. The CEO agent must stay at or under 350 lines (currently 348).

Do NOT modify any other content in ceo.md. The heavy lifting (detailed format specification) is in orchestration-protocol.md, not in the agent file.
  </action>
  <verify>
    <automated>grep -q "Threshold Assessment:" agents/ceo.md && wc -l < agents/ceo.md | xargs test 351 -gt</automated>
  </verify>
  <done>CEO Decision Record template has "Threshold Assessment" referencing the structured format in orchestration-protocol.md. CEO.md is at or under 350 lines.</done>
</task>

</tasks>

<verification>
- config/orchestration-protocol.md Step 4 references routing-table.md for structured diagnostic questions
- config/orchestration-protocol.md Step 5 has per-condition TRIGGERED/NOT TRIGGERED format for all 5 thresholds
- agents/ceo.md line 120 references "Threshold Assessment" with pointer to orchestration-protocol.md format
- agents/ceo.md is <= 350 lines
- No threshold descriptions duplicated between routing-table.md and orchestration-protocol.md (names only in protocol, full decision trees only in routing-table.md)
- All other sections of both files unchanged
</verification>

<success_criteria>
- Phase 1 threshold evaluation is structured and per-condition, not a summary statement
- CEO Decision Record template references the structured format
- No content duplication between routing-table.md and orchestration-protocol.md
- CEO.md stays at or under 350 lines
- orchestration-protocol.md grows by approximately 10-15 lines
</success_criteria>

<output>
After completion, create `.planning/phases/07-specification-formalization/07-03-SUMMARY.md`
</output>
