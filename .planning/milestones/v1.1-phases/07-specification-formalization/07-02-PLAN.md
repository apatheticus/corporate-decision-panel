---
phase: 07-specification-formalization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config/decision-modes.md
autonomous: true
requirements:
  - SPEC-04
  - SPEC-05
  - SPEC-06

must_haves:
  truths:
    - "Each of the 5 decision modes has a directional weighting table mapping all 8 C-suite perspectives to HIGH/MODERATE/LOW influence"
    - "Multi-mode cost formula shows the actual calculation with agent invocation breakdown"
    - "Worked examples cover at least 2 scenarios with concrete numbers for different mode counts and panel sizes"
    - "No numeric multipliers (1.5x, 0.7x) appear -- only directional indicators (HIGH/MODERATE/LOW)"
    - "Weighting tables describe CEO synthesis behavior, not C-suite agent behavior"
  artifacts:
    - path: "config/decision-modes.md"
      provides: "Directional weighting tables for all 5 modes plus multi-mode cost formula with worked examples"
      contains: "Influence Level"
  key_links:
    - from: "config/decision-modes.md"
      to: "agents/ceo.md"
      via: "CEO references config/decision-modes.md for synthesis weighting"
      pattern: "config/decision-modes.md"
---

<objective>
Add directional weighting tables to all 5 decision modes and expand the multi-mode cost section with the actual formula and worked examples in config/decision-modes.md.

Purpose: Makes synthesis weighting explicit (which perspectives carry HIGH/MODERATE/LOW influence per mode) and replaces the "~1.1x" approximation with a real formula that users and developers can verify.
Output: Expanded config/decision-modes.md (~250-300 lines, up from 106)
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-specification-formalization/07-RESEARCH.md

@config/decision-modes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add directional weighting tables to all 5 decision modes</name>
  <files>config/decision-modes.md</files>
  <action>
For each of the 5 decision mode sections in config/decision-modes.md, add a "Directional Weighting" subsection AFTER the existing "CEO Prompt Modifier" block. Each weighting table maps all 8 C-suite roles to a directional influence level.

Use this table format for each mode:

```
#### Directional Weighting

| C-Suite Role | Disposition | Influence Level | Rationale |
|-------------|-------------|-----------------|-----------|
```

The 8 C-suite roles and their dispositions (from the Organizational Roster):
- COO: Skeptic
- CFO: Skeptic
- CTO: Advocate
- CISO: Skeptic
- VP Sales: Advocate
- VP Delivery: Skeptic
- CAO: Systemic
- CSO: Investigative

Weighting assignments per mode (from research Section "Weighting Table Design Guidance"):

**Guardian (MaxiMin):**
- CISO: HIGH -- Primary risk identifier; must be satisfied
- CFO: HIGH -- Financial exposure must be addressed
- COO: HIGH -- Operational feasibility is gating
- VP Delivery: HIGH -- Current obligation impact is non-negotiable
- CAO: MODERATE -- Organizational absorption matters but does not gate
- CSO: MODERATE -- Evidence informs but does not drive Guardian decisions
- CTO: LOW -- Technical opportunity is secondary to risk mitigation
- VP Sales: LOW -- Revenue opportunity does not override risk concerns

**Pioneer (MaxiMax):**
- VP Sales: HIGH -- Revenue opportunity drives Pioneer decisions
- CTO: HIGH -- Technical capability unlocks opportunity
- CSO: MODERATE -- Evidence quality informs acceleration vs caution
- CAO: MODERATE -- Organizational readiness affects execution speed
- CISO: LOW -- Security concerns are engineering problems to solve
- CFO: LOW -- Financial caution is reframed as investment thesis
- COO: LOW -- Operational constraints are implementation challenges
- VP Delivery: LOW -- Current obligations can be reprioritized

**Architect (Behavioral):**
All roles: MODERATE
Add a note below the table: "*Architect mode weights by cross-domain consensus support, not by role disposition. The position that satisfies the most domain concerns carries the most weight, regardless of which role proposed it. Effective influence depends on how many peer domains each perspective's position addresses.*"

**Analyst (Hurwicz):**
All roles: MODERATE
Add a note below the table: "*Analyst mode weights by confidence level in the domain recommendation, not by role disposition. HIGH-confidence findings from any role outweigh LOW-confidence findings from any other role. Effective influence is driven by evidence quality, not organizational position.*"

**Sentinel (MiniMax Regret):**
All roles: MODERATE
Add a note below the table: "*Sentinel mode gives disproportionate weight to the single strongest objection from ANY role, regardless of that role's disposition. The question is not 'which role matters most' but 'which warning would I most regret ignoring.' Effective influence depends on the severity and plausibility of each role's strongest concern.*"

CRITICAL CONSTRAINTS:
- Use ONLY HIGH/MODERATE/LOW -- NO numeric multipliers (1.5x, 0.7x). This is explicit in REQUIREMENTS.md Out of Scope.
- Weighting tables describe CEO SYNTHESIS behavior, not C-suite agent behavior. Domain analysis is mode-independent. Add a clarifying note at the top of the first weighting table: "These tables describe how the CEO weights perspectives during synthesis. They do NOT change how C-suite agents perform their domain analysis -- domain analysis is mode-independent."
- Preserve ALL existing content (Disposition, Decision Theory, Resolution Pattern, CEO Prompt Modifier, Mode/Tier Interaction Matrix, Multi-Mode Comparison, CEO Mode Recommendation).
- For CSO, influence level represents how much the CEO weighs CSO evidence in resolving fault lines, not whether the CSO "votes."
  </action>
  <verify>
    <automated>grep -c "Influence Level" config/decision-modes.md | xargs test 5 -eq</automated>
  </verify>
  <done>All 5 decision modes have directional weighting tables with all 8 C-suite roles mapped to HIGH/MODERATE/LOW influence levels. No numeric multipliers present. Tables clearly describe CEO synthesis behavior, not C-suite agent behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Expand multi-mode cost formula with calculation and worked examples</name>
  <files>config/decision-modes.md</files>
  <action>
Replace the current Multi-Mode Comparison section content (lines 80-93 of the original file, preserving the section header and invocation patterns) with an expanded version containing:

1. **Keep** the existing first paragraph ("Domain analysis is mode-independent...") and invocation patterns list.

2. **Replace** the "Cost: Approximately 1.1x..." line with a detailed cost formula section:

```
### Multi-Mode Cost Formula

**Formula:** Total Cost = (1 x Full Domain Analysis) + (N x CEO Synthesis Pass)

Where:
- **Full Domain Analysis** = Phase 0 broadcast + Phase 1 framing + Phase 1.5 research (if CSO activated) + Phase 2 C-suite dispatch + Phase 3 team lead analysis + Phase 4 C-suite synthesis + Phase 4.5 pre-mortem (Tier 3 only)
- **CEO Synthesis Pass** = Phase 5 only (CEO reads recommendations and produces Decision Record with one mode's prompt modifier)
- **N** = number of modes requested (1 for single mode, 2 for comparison, 5 for all-modes)
```

3. **Add "Why ~1.1x" explanation:**

Express the formula generically first (K C-suite agents, L team leads total), then provide current numbers as examples. This way the formula stays correct if the roster changes.

```
### Why ~1.1x for 5 Modes

The domain analysis (Phases 0-4/4.5) is the expensive part: it involves spawning K C-suite agents, each dispatching to their team leads (L total team lead invocations for full activation). The CEO synthesis pass (Phase 5) is a single agent producing a single document from already-collected inputs -- no subagent spawning, no new analysis.

Generic formula: Cost ratio for N modes = (K + L + N) / (K + L + 1)

As K + L grows, the marginal cost of additional synthesis passes approaches zero.
```

4. **Add 3-4 worked examples** covering different scenarios:

**Example 1: Two-mode comparison (Guardian vs Pioneer), Tier 3, full activation (current roster)**
- Domain analysis (once): 1 CEO framing + 8 C-suite + 29 team leads = 38 agent invocations
- CEO synthesis (2x): 2 invocations
- Total: 40 invocations vs. 39 for single-mode = 1.03x cost

**Example 2: All-modes comparison (5 modes), Tier 3, full activation (current roster)**
- Domain analysis (once): 38 agent invocations
- CEO synthesis (5x): 5 invocations
- Total: 43 invocations vs. 39 for single-mode = 1.10x cost

**Example 3: Two-mode comparison, Tier 2, partial activation (3 C-suite, ~12 team leads)**
- Domain analysis (once): 1 + 3 + 12 = 16 agent invocations
- CEO synthesis (2x): 2 invocations
- Total: 18 invocations vs. 17 for single-mode = 1.06x cost

**Example 4: All-modes, Tier 2, partial activation (3 C-suite, ~12 team leads)**
- Domain analysis (once): 16 agent invocations
- CEO synthesis (5x): 5 invocations
- Total: 21 invocations vs. 17 for single-mode = 1.24x cost

Use the format from research:
```
**Scenario:** [description]
- Domain analysis (once): [breakdown] = [N] agent invocations
- CEO synthesis ([M]x): [M] invocations
- Total: [sum] invocations vs. [N+1] for single-mode = [ratio]x cost
```

5. **Keep** the Mode Sensitivity paragraph that currently follows the cost line, and keep it after the worked examples.

Preserve the Mode/Tier Interaction Matrix and CEO Mode Recommendation sections UNCHANGED.
  </action>
  <verify>
    <automated>grep -c "Worked Example\|Example [0-9]" config/decision-modes.md | xargs test 3 -le</automated>
  </verify>
  <done>Multi-mode cost formula section contains the actual formula with generic variables, "Why ~1.1x" explanation with generic and current-roster math, and 3-4 worked examples for different scenarios (varying mode counts and panel sizes). Mode Sensitivity paragraph preserved. No other sections modified.</done>
</task>

</tasks>

<verification>
- config/decision-modes.md contains 5 directional weighting tables (one per mode)
- Each table has 8 rows (one per C-suite role) with Disposition and Influence Level columns
- Only HIGH/MODERATE/LOW values appear in Influence Level column -- no numeric multipliers
- Architect, Analyst, and Sentinel tables have qualifying notes explaining criteria-based weighting
- A clarifying note states tables describe CEO synthesis, not C-suite behavior
- Multi-mode cost formula section has the actual formula, "Why ~1.1x" explanation, and 3+ worked examples
- Mode/Tier Interaction Matrix unchanged
- CEO Mode Recommendation table unchanged
- Existing Disposition, Decision Theory, Resolution Pattern, and CEO Prompt Modifier content preserved per mode
</verification>

<success_criteria>
- 5 weighting tables mapping 8 C-suite roles to directional influence levels
- Zero numeric multipliers in the entire file
- Cost formula expressed generically (K, L, N variables) plus worked examples with current numbers
- At least 3 worked examples with different scenarios
- All existing content preserved unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/07-specification-formalization/07-02-SUMMARY.md`
</output>
