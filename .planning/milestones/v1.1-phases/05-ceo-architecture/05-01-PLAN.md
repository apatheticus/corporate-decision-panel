---
phase: 05-ceo-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/orchestration-protocol.md
  - agents/ceo.md
  - SKILL.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02

must_haves:
  truths:
    - "config/orchestration-protocol.md exists and contains the complete five-phase cascade protocol (Phases 0 through 4.5), production pipeline, and organizational roster"
    - "agents/ceo.md is under 350 lines and contains only identity, judgment, synthesis, triage, multi-mode comparison, susceptibility mitigation, tier behavior, mode/tier matrix, and config references"
    - "Zero orchestration phase definitions remain in agents/ceo.md -- only brief 2-3 sentence summaries per phase in a reference section"
    - "config/orchestration-protocol.md references config/routing-table.md and config/decision-modes.md instead of duplicating their content"
    - "SKILL.md references config/orchestration-protocol.md where it describes the cascade"
  artifacts:
    - path: "config/orchestration-protocol.md"
      provides: "Authoritative orchestration protocol with all phase definitions, production pipeline, and roster"
      contains: "Phase 0 -- Shared Consciousness Broadcast"
    - path: "agents/ceo.md"
      provides: "Refactored CEO focused on identity, judgment, and synthesis"
      min_lines: 250
    - path: "SKILL.md"
      provides: "Updated skill documentation with orchestration-protocol.md reference"
  key_links:
    - from: "agents/ceo.md"
      to: "config/orchestration-protocol.md"
      via: "Orchestration Protocol Reference section with 2-3 sentence phase summaries"
      pattern: "config/orchestration-protocol\\.md"
    - from: "config/orchestration-protocol.md"
      to: "config/routing-table.md"
      via: "Reference instead of inline routing table duplication"
      pattern: "config/routing-table\\.md"
    - from: "SKILL.md"
      to: "config/orchestration-protocol.md"
      via: "Updated reference where cascade is described"
      pattern: "config/orchestration-protocol\\.md"
---

<objective>
Extract the orchestration protocol from the monolithic 682-line CEO agent into a separate config document, leaving the CEO focused on identity, judgment, and synthesis under 350 lines.

Purpose: ARCH-01 and ARCH-02 require clean separation of orchestration (phase sequencing, routing, production pipeline, roster) from identity/synthesis (triage, multi-mode comparison, susceptibility mitigation, tier behavior, deliberation). This extraction is the foundation for Phase 5 -- the C-suite executive summary work in Plan 02 depends on the CEO's refactored synthesis section.
Output: `config/orchestration-protocol.md` (new), refactored `agents/ceo.md` (under 350 lines), updated `SKILL.md`
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ceo-architecture/05-CONTEXT.md
@.planning/phases/05-ceo-architecture/05-RESEARCH.md

@agents/ceo.md
@config/routing-table.md
@config/decision-modes.md
@SKILL.md

<interfaces>
<!-- Current CEO.md section map (682 lines) from RESEARCH.md -->
<!-- This is the authoritative STAY/EXTRACT boundary: -->

STAYS in CEO.md (identity, judgment, synthesis):
  Lines   1-  8 (  8 lines): YAML frontmatter + title
  Lines   9- 24 ( 16 lines): ## Your Identity and Mandate
  Lines 235-360 (126 lines): ### Phase 5 -- CEO Deliberation (Synthesis)
  Lines 361-429 ( 69 lines): ## /evaluate -- Issue Triage Logic
  Lines 430-491 ( 62 lines): ## Multi-Mode Comparison
  Lines 492-521 ( 30 lines): ## Susceptibility Mitigation
  Lines 522-548 ( 27 lines): ## Tier-Specific Behavior
  Lines 658-673 ( 16 lines): ## Mode/Tier Interaction Matrix
  Lines 674-682 (  9 lines): ## Configuration References

EXTRACTED to config/orchestration-protocol.md:
  Lines  25- 28 (  4 lines): ## The Five-Phase Cascade (header)
  Lines  29- 36 (  8 lines): ### Company Context Loading
  Lines  37- 57 ( 21 lines): ### Phase 0 -- Shared Consciousness Broadcast
  Lines  58-135 ( 78 lines): ### Phase 1 -- Frame and Route
  Lines 136-172 ( 37 lines): ### Phase 1.5 -- CSO Research Directive
  Lines 173-185 ( 13 lines): ### Phase 2 -- C-Suite Dispatches Downward
  Lines 186-195 ( 10 lines): ### Phase 3 -- Team Leads Produce Findings
  Lines 196-211 ( 16 lines): ### Phase 4 -- C-Suite Synthesizes Upward
  Lines 212-234 ( 23 lines): ### Phase 4.5 -- Pre-Mortem Dispatch
  Lines 549-622 ( 74 lines): ## Production Pipeline Trigger
  Lines 623-657 ( 35 lines): ## The Organizational Roster

<!-- Existing config files (reference, not duplicate): -->
<!-- config/routing-table.md: Decision-type activation rules, threshold conditions -->
<!-- config/decision-modes.md: Five mode definitions, mode/tier interaction matrix -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create orchestration protocol and refactor CEO agent</name>
  <files>config/orchestration-protocol.md, agents/ceo.md</files>
  <action>
Create `config/orchestration-protocol.md` by extracting the following sections VERBATIM from `agents/ceo.md`:

1. Add a header section:
   ```
   # CDP Orchestration Protocol

   This is the authoritative phase sequencing document for the Corporate Decision Panel.
   It defines the five-phase cascade that governs all Tier 2 and Tier 3 engagements.

   **Referenced configuration:**
   - `config/routing-table.md` -- Decision-type activation rules and full-activation threshold conditions
   - `config/decision-modes.md` -- Five CEO synthesis prompt modifiers and mode/tier interaction matrix
   - `config/company-profile.md` -- Company archetype presets and override mechanism
   ```

2. Extract these sections from CEO.md into the new file, preserving their content:
   - Company Context Loading (lines 29-36)
   - Phase 0 -- Shared Consciousness Broadcast (lines 37-57)
   - Phase 1 -- Frame and Route (lines 58-135) -- BUT replace the inline routing table in Step 3 with: "Apply the default routing table from `config/routing-table.md`:" followed by a brief note: "See `config/routing-table.md` for the complete default activation table and CSO activation patterns. You always participate. You may override defaults by adding or removing C-suite members. State override reasoning explicitly." Keep Steps 1, 2, 4, 5 verbatim. The threshold conditions in Step 4 stay in the protocol (they are orchestration logic, not routing data).
   - Phase 1.5 -- CSO Research Directive (lines 136-172)
   - Phase 2 -- C-Suite Dispatches Downward (lines 173-185)
   - Phase 3 -- Team Leads Produce Findings (lines 186-195)
   - Phase 4 -- C-Suite Synthesizes Upward (lines 196-211)
   - Phase 4.5 -- Pre-Mortem Dispatch (lines 212-234)
   - Production Pipeline Trigger (lines 549-622)
   - The Organizational Roster (lines 623-657)

3. In `config/orchestration-protocol.md`, maintain the heading hierarchy: use `##` for major phases and `###` for subsections, matching the original structure. Add `---` separators between major sections as in the original.

Then refactor `agents/ceo.md`:

4. Remove all extracted sections from CEO.md.

5. Add a new `## Orchestration Protocol Reference` section immediately after `## Your Identity and Mandate` (after line 24). This section contains:
   - An introductory sentence: "The full orchestration protocol is defined in `config/orchestration-protocol.md`. This section provides a brief overview so the CEO understands the flow without embedding the full protocol."
   - Exactly 2-3 sentences per phase (Company Context Loading, Phase 0, Phase 1, Phase 1.5, Phase 2, Phase 3, Phase 4, Phase 4.5). Use the summaries from the RESEARCH.md "CEO Phase Summary Reference Section" as the template -- they are already at the right length.
   - A closing line: "For production pipeline trigger, session setup, spawn sequence, and organizational roster details, see `config/orchestration-protocol.md`."
   - Target: ~30 lines for this entire section.

6. Rename "### Phase 5 -- CEO Deliberation (Synthesis)" to "## CEO Deliberation (Synthesis)" (promote to h2 since it is no longer under a "Five-Phase Cascade" parent). Keep all Phase 5 content unchanged for now (Plan 02 will update the synthesis logic).

7. Update the `## Configuration References` section to add orchestration-protocol.md:
   - Add: `- **Orchestration protocol:** \`config/orchestration-protocol.md\` -- Five-phase cascade protocol, production pipeline trigger, session setup, organizational roster`

8. Clean up: Remove any transition text, orphaned `---` separators, or redundant introductory lines that were part of the extracted sections. Ensure no blank line gaps exceed 2 consecutive blank lines.

CRITICAL CONSTRAINTS (from locked decisions):
- `/evaluate` triage logic STAYS in CEO -- do not extract
- Multi-mode comparison STAYS in CEO -- do not extract
- Susceptibility mitigation STAYS in CEO -- do not extract
- Tier-specific behavior STAYS in CEO -- do not extract
- Mode/tier interaction matrix STAYS in CEO -- do not extract
- Config references section STAYS in CEO -- do not extract
- Zero duplication: phase summaries in the reference section must be brief overviews (2-3 sentences each), NOT reproductions of protocol steps
  </action>
  <verify>
    <automated>bash -c 'LINES=$(wc -l < agents/ceo.md); echo "CEO lines: $LINES"; [ "$LINES" -lt 350 ] && echo "PASS: under 350" || echo "FAIL: over 350"; test -f config/orchestration-protocol.md && echo "PASS: protocol exists" || echo "FAIL: protocol missing"; grep -c "config/orchestration-protocol.md" agents/ceo.md | xargs -I{} bash -c "[ {} -ge 2 ] && echo \"PASS: CEO references protocol\" || echo \"FAIL: CEO missing protocol reference\""; grep -q "Phase 0" config/orchestration-protocol.md && echo "PASS: Phase 0 in protocol" || echo "FAIL: Phase 0 missing"; grep -q "Phase 4.5" config/orchestration-protocol.md && echo "PASS: Phase 4.5 in protocol" || echo "FAIL: Phase 4.5 missing"; grep -q "Production Pipeline" config/orchestration-protocol.md && echo "PASS: Production in protocol" || echo "FAIL: Production missing"; grep -q "Organizational Roster" config/orchestration-protocol.md && echo "PASS: Roster in protocol" || echo "FAIL: Roster missing"; grep -q "## Orchestration Protocol Reference" agents/ceo.md && echo "PASS: Reference section exists" || echo "FAIL: Reference section missing"; grep -q "## CEO Deliberation" agents/ceo.md && echo "PASS: Synthesis section exists" || echo "FAIL: Synthesis missing"; grep -q "/evaluate" agents/ceo.md && echo "PASS: Triage stayed" || echo "FAIL: Triage missing"; grep -q "Multi-Mode Comparison" agents/ceo.md && echo "PASS: Multi-mode stayed" || echo "FAIL: Multi-mode missing"; grep -q "Susceptibility Mitigation" agents/ceo.md && echo "PASS: Susceptibility stayed" || echo "FAIL: Susceptibility missing"'</automated>
  </verify>
  <done>
- config/orchestration-protocol.md exists with all extracted orchestration content (Phases 0-4.5, production pipeline, roster)
- agents/ceo.md is under 350 lines
- CEO.md contains: identity, orchestration reference (brief), synthesis (Phase 5), triage, multi-mode, susceptibility, tier behavior, mode/tier matrix, config references
- CEO.md references config/orchestration-protocol.md in at least 2 places (reference section + config references)
- Zero duplicated orchestration content between the two files
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md cascade references</name>
  <files>SKILL.md</files>
  <action>
Update SKILL.md to reference config/orchestration-protocol.md where the cascade is described. The SKILL.md already describes the phases at a summary level (lines ~233-281 describe the cascade), so it does NOT need its content changed -- just add pointers to the new authoritative source.

Specific changes:

1. Find the section around line 233 where the five-phase cascade is described. Add a brief note at the start of that section (or near the "Full five-phase cascade" description): "The authoritative phase protocol is defined in `config/orchestration-protocol.md`. The overview below describes the flow at a summary level."

2. Find the reference to `agents/ceo.md` around line 665 ("agents/ceo.md -- CEO with five-phase cascade protocol"). Update to: "agents/ceo.md -- CEO identity, judgment criteria, and synthesis logic" and add a new line: "config/orchestration-protocol.md -- Five-phase cascade protocol, production pipeline, organizational roster"

3. Do NOT rewrite SKILL.md content. Only add reference pointers. The SKILL.md is a user-facing document; minimal changes preserve stability.

Per RESEARCH.md Open Question 1 recommendation: "Update SKILL.md minimally -- add a reference to config/orchestration-protocol.md where it describes the cascade."
  </action>
  <verify>
    <automated>bash -c 'grep -q "config/orchestration-protocol.md" SKILL.md && echo "PASS: SKILL.md references protocol" || echo "FAIL: SKILL.md missing protocol reference"; grep -c "orchestration-protocol" SKILL.md | xargs -I{} bash -c "[ {} -ge 2 ] && echo \"PASS: multiple references\" || echo \"FAIL: only one reference\""'</automated>
  </verify>
  <done>
- SKILL.md references config/orchestration-protocol.md in at least 2 places
- SKILL.md file listing updated to include orchestration-protocol.md
- No substantive content changes to SKILL.md beyond adding pointers
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `wc -l agents/ceo.md` returns under 350
2. `config/orchestration-protocol.md` exists and contains Phase 0 through 4.5, Production Pipeline, and Organizational Roster
3. `agents/ceo.md` contains: Identity and Mandate, Orchestration Protocol Reference, CEO Deliberation (Synthesis), /evaluate, Multi-Mode Comparison, Susceptibility Mitigation, Tier-Specific Behavior, Mode/Tier Interaction Matrix, Configuration References
4. `grep -r "five-phase cascade\|Phase 0\|Phase 1 --\|Phase 2 --\|Phase 3 --\|Phase 4 --" agents/ceo.md` returns only brief summary mentions (in the reference section), not full protocol definitions
5. `SKILL.md` references `config/orchestration-protocol.md`
6. Existing pytest suite passes: `pytest tests/ -x -m "not live"` (no Python changes, but sanity check)
</verification>

<success_criteria>
- CEO agent file is under 350 lines, focused on identity, judgment, and synthesis
- Orchestration protocol document exists as the single authoritative source for phase sequencing
- Zero duplication of orchestration logic between the two documents
- All cross-references updated (CEO -> protocol, SKILL.md -> protocol, protocol -> routing-table, protocol -> decision-modes)
- ARCH-01 and ARCH-02 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-ceo-architecture/05-01-SUMMARY.md`
</output>
