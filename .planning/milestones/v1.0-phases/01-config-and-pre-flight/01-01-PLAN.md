---
phase: 01-config-and-pre-flight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - requirements-dev.txt
  - scripts/__init__.py
  - scripts/config.py
  - templates/config-context.md
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
autonomous: true
requirements: [SETUP-01, SETUP-03, DOC-02]

must_haves:
  truths:
    - "Config template contains Gemini API Key, Image Model, and Retry Limit fields with placeholder-with-instructions format"
    - "Config template does NOT contain the Platform field"
    - "Config parser reads API key, model ID, and retry limit from .cdp-context/config.md"
    - "Missing or empty fields produce clear error messages"
    - "Unfilled placeholder values like '(paste your key here)' are treated as empty"
    - "Model ID defaults to gemini-2.5-flash-image when not set or placeholder"
    - "Retry limit defaults to 2 when not set or placeholder"
  artifacts:
    - path: "templates/config-context.md"
      provides: "Updated config template with API key, model ID, retry limit fields"
      contains: "Gemini API Key"
    - path: "scripts/config.py"
      provides: "Config parser module"
      exports: ["parse_config", "load_config", "ConfigError"]
    - path: "scripts/__init__.py"
      provides: "Package init enabling cross-module imports"
    - path: "requirements.txt"
      provides: "Python runtime dependencies"
      contains: "google-genai"
    - path: "requirements-dev.txt"
      provides: "Python dev dependencies"
      contains: "pytest"
    - path: "tests/test_config.py"
      provides: "Config parser and template tests"
      min_lines: 50
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
  key_links:
    - from: "scripts/config.py"
      to: ".cdp-context/config.md"
      via: "parse_config reads markdown fields"
      pattern: "FIELD_PATTERN.*finditer"
    - from: "tests/test_config.py"
      to: "scripts/config.py"
      via: "imports parse_config, load_config, ConfigError"
      pattern: "from scripts.config import"
    - from: "tests/test_config.py"
      to: "templates/config-context.md"
      via: "validates template contains required fields"
      pattern: "config-context.md"
---

<objective>
Create the config template, config parser, and test infrastructure for Phase 1.

Purpose: Establish the configuration foundation that pre-flight validation (Plan 02) and all subsequent phases depend on. The config parser is the single entry point for reading API key, model ID, and retry limit from the user's `.cdp-context/config.md` file.

Output: Updated config template, working config parser with error handling, pytest test suite covering parsing and template validation.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-config-and-pre-flight/01-CONTEXT.md
@.planning/phases/01-config-and-pre-flight/01-RESEARCH.md
@.planning/phases/01-config-and-pre-flight/01-VALIDATION.md

@templates/config-context.md

<interfaces>
<!-- Current config template (to be replaced): -->
From templates/config-context.md:
```markdown
# CDP Configuration
> **Usage:** Copy this file to `.cdp-context/config.md` and set your preferences.
## Image Generation Platform
- **Platform:** (gemini, chatgpt -- default: gemini)
```

<!-- Existing config field format from CONTEXT.md: -->
Markdown field format: `- **Field Name:** (value -- default: x)` with parenthetical defaults/options

<!-- Regex pattern from RESEARCH.md: -->
```python
FIELD_PATTERN = re.compile(r'^-\s+\*\*(.+?):\*\*\s*(.*)$', re.MULTILINE)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project infrastructure and config template</name>
  <files>requirements.txt, requirements-dev.txt, scripts/__init__.py, tests/__init__.py, tests/conftest.py, templates/config-context.md</files>
  <action>
1. Create `requirements.txt` with runtime dependencies:
   ```
   google-genai>=1.65.0
   Pillow>=10.0.0
   ```

2. Create `requirements-dev.txt` with dev dependencies:
   ```
   -r requirements.txt
   pytest>=8.0.0
   ```

3. Create `scripts/__init__.py` as an empty file (makes scripts/ a package for cross-module imports in later phases).

4. Create `tests/__init__.py` as an empty file.

5. Create `tests/conftest.py` with shared fixtures:
   - `tmp_config(tmp_path)` fixture: creates a temporary `.cdp-context/config.md` file from provided content string, returns path to the config file. Accepts a `content` parameter via indirect parametrization or a factory pattern (e.g., `make_config` fixture that returns a callable accepting content string).
   - `valid_config_content()` fixture: returns a string with all three fields properly filled: `Gemini API Key: test-key-abc123`, `Image Model: gemini-2.5-flash-image`, `Retry Limit: 3`.
   - `template_path()` fixture: returns `Path("templates/config-context.md")` for template validation tests.

6. Replace `templates/config-context.md` entirely. Remove the Platform field. New content:

```markdown
# CDP Configuration

> **Usage:** Copy this file to `.cdp-context/config.md` and fill in your API key.
> All other settings have defaults -- only change what you need.
>
> This file is gitignored by default (it lives in `.cdp-context/`).
>
> Get a Gemini API key at: https://aistudio.google.com/apikey

---

## Image Generation

- **Gemini API Key:** (paste your key here)
- **Image Model:** (default: gemini-2.5-flash-image)
- **Retry Limit:** (default: 2)
```

Note on default model: CONTEXT.md locked `gemini-2.0-flash-exp` but RESEARCH.md confirms this model was shut down Nov 14, 2025. Using `gemini-2.5-flash-image` per research recommendation -- the current stable image generation model.

7. Install dev dependencies: `pip install -r requirements-dev.txt`
  </action>
  <verify>
    <automated>python -c "import pytest; print('pytest available')" && python -c "from pathlib import Path; t = Path('templates/config-context.md').read_text(); assert 'Gemini API Key' in t; assert 'Image Model' in t; assert 'Retry Limit' in t; assert 'Platform' not in t; print('template OK')" && python -c "from pathlib import Path; assert Path('scripts/__init__.py').exists(); assert Path('tests/__init__.py').exists(); assert Path('tests/conftest.py').exists(); print('structure OK')"</automated>
  </verify>
  <done>
  - requirements.txt and requirements-dev.txt exist with correct dependencies
  - scripts/ and tests/ are Python packages (have __init__.py)
  - tests/conftest.py has shared fixtures for config testing
  - templates/config-context.md has Gemini API Key, Image Model, Retry Limit fields; Platform field is gone
  - pytest is installed and importable
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Config parser with tests</name>
  <files>scripts/config.py, tests/test_config.py</files>
  <behavior>
  - parse_config(path) extracts "gemini api key", "image model", "retry limit" from a valid config file
  - parse_config returns lowercase field names as dict keys, stripped values
  - Values starting with "(" (unfilled placeholders) are treated as empty string
  - parse_config raises ConfigError if config file does not exist
  - load_config(config_dir) is the high-level function: calls parse_config, validates required fields, applies defaults, returns a typed dict with keys "api_key", "model_id", "retry_limit"
  - load_config raises ConfigError with specific message if API key is missing or placeholder
  - load_config defaults model_id to "gemini-2.5-flash-image" when field is empty or placeholder
  - load_config defaults retry_limit to 2 when field is empty or placeholder
  - load_config converts retry_limit to int; raises ConfigError if not a valid integer
  - ConfigError is a custom exception with error_code and remediation attributes
  - Template file (templates/config-context.md) contains all three required fields: "Gemini API Key", "Image Model", "Retry Limit"
  - Template file does NOT contain "Platform" field
  </behavior>
  <action>
Create `tests/test_config.py` first (RED phase):
- test_parse_valid_config: all three fields extracted correctly
- test_parse_placeholder_values: "(paste your key here)" treated as empty
- test_parse_default_extraction: "(default: 2)" parsed to extract "2" as the value
- test_parse_missing_file: raises ConfigError
- test_load_config_valid: returns dict with api_key, model_id, retry_limit
- test_load_config_missing_key: raises ConfigError with "CONFIG_MISSING_KEY" code
- test_load_config_default_model: defaults to "gemini-2.5-flash-image"
- test_load_config_default_retry: defaults to 2
- test_load_config_invalid_retry: raises ConfigError for non-integer retry limit
- test_template_has_required_fields: reads templates/config-context.md, asserts all three fields present
- test_template_no_platform_field: reads templates/config-context.md, asserts "Platform" not present

Run tests (expect failures).

Then create `scripts/config.py` (GREEN phase):
- Define `ConfigError(Exception)` with `error_code`, `message`, `remediation` attributes
- Define `FIELD_PATTERN = re.compile(r'^-\s+\*\*(.+?):\*\*\s*(.*)$', re.MULTILINE)` for markdown field parsing
- Implement `parse_config(config_path: Path) -> dict[str, str]`: reads file, extracts fields via regex, returns lowercase-key dict. Raises ConfigError if file missing.
- Implement `_extract_value(raw: str) -> str`: if value starts with "(", check for `(default: X)` pattern and extract X; otherwise return empty string. If value doesn't start with "(", return stripped value.
- Implement `load_config(config_dir: Path) -> dict[str, Any]`: calls parse_config on `config_dir / "config.md"`, validates API key present and not placeholder, applies defaults for model_id and retry_limit, converts retry_limit to int, returns `{"api_key": str, "model_id": str, "retry_limit": int}`.

Run tests (expect pass).
  </action>
  <verify>
    <automated>python -m pytest tests/test_config.py -v</automated>
  </verify>
  <done>
  - All test_config.py tests pass
  - parse_config correctly extracts fields from markdown format
  - Placeholder values are handled (not passed through as literal strings)
  - load_config validates API key, applies defaults, converts types
  - ConfigError provides structured error info (code, message, remediation)
  - Template validation tests confirm correct fields
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_config.py -v` -- all tests pass
- `python -c "from scripts.config import parse_config, load_config, ConfigError; print('imports OK')"` -- module is importable
- templates/config-context.md contains "Gemini API Key", "Image Model", "Retry Limit" and does NOT contain "Platform"
</verification>

<success_criteria>
- Config template updated with all three fields, Platform removed
- Config parser handles valid input, placeholder detection, defaults, and type conversion
- Test suite covers happy path, edge cases, and error conditions
- Module structure supports future imports from other scripts
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-and-pre-flight/01-01-SUMMARY.md`
</output>
