---
phase: 04-scale-and-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/production/infographics.md
  - agents/ceo.md
autonomous: true
requirements:
  - DOC-01
  - DOC-03

must_haves:
  truths:
    - "infographics.md Technology section describes scripts/session.py and google-genai SDK, not browser automation"
    - "infographics.md Workflow section describes 4-step API script workflow, not 8-step browser cycle"
    - "infographics.md Retry Behavior section references config.md Retry Limit field, not hard conversation limits"
    - "infographics.md Error Handling section describes placeholder PNG, content blocks, rate limiting, session summary, and pipeline non-blocking behavior"
    - "ceo.md Task A spawn says 'Generate analytical infographics via Gemini API script' not 'via browser automation'"
    - "All 6 infographic specifications, Output Requirements, Content Mapping, and Multi-Mode Variant sections are preserved unchanged"
  artifacts:
    - path: "templates/production/infographics.md"
      provides: "API-based Image Agent specification"
      contains: "scripts/session.py"
    - path: "agents/ceo.md"
      provides: "Updated Task A spawn instruction"
      contains: "Gemini API script"
  key_links:
    - from: "templates/production/infographics.md"
      to: "scripts/session.py"
      via: "Technology and Workflow sections reference session orchestrator"
      pattern: "scripts/session\\.py"
    - from: "agents/ceo.md"
      to: "scripts/session.py"
      via: "Task A spawn instruction references session runner"
      pattern: "scripts/session\\.py"
---

<objective>
Rewrite the infographics.md Task A spec and ceo.md Task A spawn instruction for the API-based workflow.

Purpose: Replace browser automation instructions with API script workflow so the Image Agent calls scripts/session.py instead of navigating to gemini.google.com/chatgpt.com. This is the primary documentation deliverable of Phase 4.

Output: Updated templates/production/infographics.md with 4 sections replaced (Technology, Attempt Budget, Browser Automation Workflow, Error Handling) and updated agents/ceo.md Task A spawn text.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@templates/production/infographics.md
@agents/ceo.md
@.planning/phases/04-scale-and-docs/04-CONTEXT.md
@.planning/phases/04-scale-and-docs/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite infographics.md for API-based workflow</name>
  <files>templates/production/infographics.md</files>
  <action>
Rewrite 4 sections of `templates/production/infographics.md` while preserving all other sections exactly as-is. The file is 363 lines. Sections to replace and their replacements:

**1. Technology section (lines 22-68) -- REPLACE ENTIRELY with:**

```markdown
## Technology

**Generation engine:** `scripts/session.py` -- Python session orchestrator
that calls the Gemini API directly via `google-genai` SDK. No browser
automation.

**Prompt format:** JSON templates in `templates/infographic-prompts/`
with six top-level keys: `core`, `style`, `technical`, `composition`,
`quality_keywords`, `extras`.

**Style overrides:** If `.cdp-context/style.md` exists, the script reads
it and appends style guidance to each prompt automatically.

**Configuration:** `.cdp-context/config.md` provides:
- **Gemini API Key** -- required for API access
- **Image Model** -- default: gemini-2.5-flash-image
- **Retry Limit** -- default: 2 (3 total attempts per infographic)
```

Remove: Platform Profiles table, Prompt Population Workflow 5-step list, Style Configuration Integration mapping table. Replace all of that with the above.

**2. Attempt Budget section (lines 72-90) -- REPLACE with Retry Behavior:**

```markdown
## Retry Behavior

Retry limit is configured in `.cdp-context/config.md` via the
**Retry Limit** field (default: 2, meaning 3 total attempts per
infographic). The script handles retries internally:

- **Transient errors** (429, 500, 503) trigger exponential backoff
  with jitter
- **Content/safety blocks** produce a placeholder PNG immediately
  (no retry)
- **Validation failures** retry with corrective feedback appended
  to the prompt
- **Budget exhaustion** saves the image as-is (if generated) or
  produces a placeholder PNG with a saved prompt JSON for manual retry
```

Remove: Hard limits warning, per-infographic 3-attempt conversation rules, session-wide 12-submission limit, conversation rule, tracking instruction.

**3. Browser Automation Workflow section (lines 93-140) -- REPLACE with Generation Workflow:**

```markdown
## Generation Workflow

For each session, the Image Agent follows this workflow:

1. **Extract data** -- Read the Decision Record and extract the required
   data for each infographic type per the Content Mapping table below
2. **Write data files** -- Save one JSON file per infographic type to
   `{session-output}/images/` with placeholder token values
3. **Run session** -- Call `scripts/session.py` with all type slugs,
   data paths, and the output directory. The session orchestrator:
   - Generates each type sequentially with 4-second inter-call delay
   - Runs vision-based validation after each successful generation
   - Retries with corrective feedback if validation fails
   - Doubles inter-call delay if 429 rate limit is encountered
   - Produces a summary table with status per type
4. **Report results** -- Parse the session summary and report
   OK / OK+WARN / FAILED / BLOCKED status per type to the CEO agent
```

Remove: 8-step browser cycle (Navigate, Select mode, Submit prompt, Wait, Inspect, Iterate, Download/placeholder, New conversation).

**4. Error Handling section (lines 326-349) -- REPLACE with:**

```markdown
## Error Handling

1. **Placeholder on failure** -- When all attempts for an infographic
   are exhausted without a successful generation, a placeholder PNG is
   created (white background, centered error text) and the prompt is
   saved as `INFOGRAPHIC_<type-slug>_PROMPT.json` for manual retry.
2. **Content blocks** -- If the Gemini API blocks the prompt for
   content/safety reasons, a placeholder is generated immediately
   with no retry (the same prompt will always be blocked).
3. **Rate limiting** -- 429 responses trigger exponential backoff.
   The session orchestrator also doubles its inter-call delay for
   remaining types to reduce further rate limit hits.
4. **Session summary** -- After all types are processed, the script
   prints a summary table: `SUMMARY <type> <status> <attempts>/<max> <path>`
   where status is OK, OK+WARN, FAILED, or BLOCKED.
5. **Never block the pipeline** -- A PNG file exists at the standard
   path for every type regardless of outcome (real or placeholder)
   so downstream agents (Tasks B, C, D) are never blocked.
```

Remove: "Check session budget" step, conversation references, "12 total" budget, log status with attempt 2/3 breakdown.

**CRITICAL: Preserve these sections EXACTLY as-is:**
- Purpose (lines 1-19)
- Infographic Specifications (lines 144-290) -- all 6 types
- Output Requirements (lines 292-311)
- Multi-Mode Variant (lines 314-323)
- Content Mapping from Decision Record (lines 352-363)

Keep all `---` horizontal rule separators between major sections.
  </action>
  <verify>
    <automated>grep -c "browser.automation\|Browser Automation\|chatgpt\.com\|gemini\.google\.com\|Platform Profile\|model picker\|fast-mode\|ChatGPT\|platform selection" templates/production/infographics.md | grep -q "^0$" && echo "PASS: No browser automation references" || echo "FAIL: Browser automation references remain"</automated>
  </verify>
  <done>infographics.md has 4 sections replaced (Technology, Retry Behavior, Generation Workflow, Error Handling) with API-based content. All 6 infographic specifications, Output Requirements, Content Mapping, and Multi-Mode Variant sections are unchanged. Zero browser automation references remain in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Update ceo.md Task A spawn instruction</name>
  <files>agents/ceo.md</files>
  <action>
In `agents/ceo.md`, find lines 594-599 containing the Task A spawn command and replace them.

**Current text (lines 594-599):**
```
TaskCreate: "Generate analytical infographics via browser automation
  Read .cdp-context/config.md for platform selection (gemini or chatgpt)
  Use JSON prompt templates from templates/infographic-prompts/
  Read .cdp-context/style.md for visual style overrides if present
  Session output: <absolute-path>  Issue slug: <issue-slug>"            -> Task A
```

**Replacement text:**
```
TaskCreate: "Generate analytical infographics via Gemini API script
  Extract data from Decision Record per infographic type
  Write data JSON files to {session}/images/ per type
  Run scripts/session.py to generate all types
  Session output: <absolute-path>  Issue slug: <issue-slug>"            -> Task A
```

Changes: Line 595 "browser automation" -> "Gemini API script". Lines 596-598 replace platform selection + template reference + style reference with data extraction + JSON writing + session.py invocation. Line 599 (session output + arrow) stays the same. The `-> Task A` alignment with other TaskCreate lines must be preserved.

Do NOT modify any other spawn commands (Tasks B, C, D, E) or any other content in ceo.md.
  </action>
  <verify>
    <automated>grep -n "browser.automation\|platform selection" agents/ceo.md | wc -l | grep -q "^0$" && grep -q "Gemini API script" agents/ceo.md && echo "PASS: ceo.md updated" || echo "FAIL: ceo.md not updated correctly"</automated>
  </verify>
  <done>ceo.md Task A spawn instruction says "Generate analytical infographics via Gemini API script" with data extraction, JSON writing, and scripts/session.py invocation. No browser automation or platform selection references. Other spawn commands unchanged.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "browser.automation\|Browser Automation\|chatgpt\.com\|gemini\.google\.com\|Platform Profile\|model picker\|ChatGPT" templates/production/infographics.md` returns 0
2. `grep -c "browser.automation\|platform selection" agents/ceo.md` returns 0
3. `grep -c "scripts/session.py" templates/production/infographics.md` returns at least 1
4. `grep -c "Gemini API script" agents/ceo.md` returns 1
5. All 6 infographic specification headings still present: `grep -c "### [1-6]\." templates/production/infographics.md` returns 6
6. `python -m pytest tests/ -x -m "not live"` passes (no Python code changed, but confirms nothing broke)
</verification>

<success_criteria>
- infographics.md Technology section describes scripts/session.py and google-genai SDK
- infographics.md has Generation Workflow (4-step), not Browser Automation Workflow (8-step)
- infographics.md has Retry Behavior referencing config.md, not Attempt Budget with hard limits
- infographics.md Error Handling describes placeholder PNGs, content blocks, rate limiting, session summary
- ceo.md Task A spawn references "Gemini API script" and scripts/session.py
- All 6 infographic specifications preserved unchanged
- Output Requirements, Content Mapping, Multi-Mode Variant preserved unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-scale-and-docs/04-01-SUMMARY.md`
</output>
