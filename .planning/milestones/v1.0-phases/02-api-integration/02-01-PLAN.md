---
phase: 02-api-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/generate_infographic.py
  - tests/test_generate_infographic.py
  - tests/conftest.py
autonomous: true
requirements: [GEN-02]

must_haves:
  truths:
    - "JSON prompt templates are flattened to natural language paragraphs"
    - "Data placeholders ({{TOKEN}}) are replaced with actual values from a data JSON file"
    - "Hex color codes from extras.color_mapping appear in the assembled prompt"
    - "Prompt is saved to PROMPT.txt file alongside generation output"
    - "Style overrides from .cdp-context/style.md are appended when file exists"
  artifacts:
    - path: "scripts/generate_infographic.py"
      provides: "Template loading, placeholder substitution, prompt serialization"
      exports: ["load_template", "serialize_template", "substitute_placeholders"]
      contains: "PLACEHOLDER_RE"
    - path: "tests/test_generate_infographic.py"
      provides: "TestPromptSerialization test class"
      min_lines: 60
    - path: "tests/conftest.py"
      provides: "Fixtures for sample template data and data JSON"
      contains: "sample_template"
  key_links:
    - from: "scripts/generate_infographic.py"
      to: "templates/infographic-prompts/*.json"
      via: "load_template reads JSON by type slug"
      pattern: "TEMPLATE_DIR.*json"
    - from: "scripts/generate_infographic.py"
      to: "scripts/config.py"
      via: "imports ConfigError for structured errors"
      pattern: "from scripts.config import ConfigError"
---

<objective>
Build the prompt serialization pipeline: load JSON templates, substitute data placeholders, flatten to natural language, include hex colors, and optionally append style.md overrides.

Purpose: Prompt assembly is the core transformation -- template JSON + data JSON = natural language prompt for Gemini. Getting this right (and tested) before wiring the API call prevents debugging prompt issues through the API layer.

Output: Importable functions for template loading and prompt serialization, with a TDD test suite proving correctness.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-integration/02-CONTEXT.md
@.planning/phases/02-api-integration/02-RESEARCH.md

@scripts/config.py
@tests/conftest.py
@templates/infographic-prompts/domain-scorecard.json
@templates/infographic-prompts/routing-diagram.json

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From scripts/config.py:
```python
class ConfigError(Exception):
    def __init__(self, error_code: str, message: str, remediation: str = "") -> None: ...

def load_config(config_dir: Path) -> dict[str, Any]:
    """Returns dict with keys: api_key (str), model_id (str), retry_limit (int)."""
```

From tests/conftest.py:
```python
@pytest.fixture
def make_config(tmp_path):
    """Factory: make_config(content) -> config_dir Path."""

@pytest.fixture
def valid_config_content():
    """Returns a config string with all three fields."""
```

Template JSON structure (all 6 templates follow this):
```json
{
  "core": {
    "subject": "string -- infographic description",
    "objects": ["{{PLACEHOLDER}} -- description", ...],
    "notes": ["{{PLACEHOLDER}} -- description", ...],
    "constraints": ["text", ...]
  },
  "style": { "primary_style": "str", "render_quality": "str", "lighting": "str", "color_profile": "str" },
  "technical": { "resolution": "str", "anti_aliasing": bool, "noise": "str", "color_depth": "str" },
  "composition": { "perspective": "str", "framing": "str" },
  "quality_keywords": { "include": ["str", ...], "avoid": ["str", ...] },
  "extras": {
    "color_mapping": { "name": "#hex", ... },
    "data": { "key": "{{PLACEHOLDER}} -- description", ... }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prompt serialization with TDD</name>
  <files>scripts/generate_infographic.py, tests/test_generate_infographic.py, tests/conftest.py</files>
  <behavior>
    - load_template("domain-scorecard") reads templates/infographic-prompts/domain-scorecard.json and returns dict
    - load_template("domain_scorecard") normalizes to "domain-scorecard" (underscore to hyphen)
    - load_template("nonexistent-type") raises ConfigError with TEMPLATE_NOT_FOUND error_code
    - substitute_placeholders replaces {{TOKEN}} with data dict values, unreplaced tokens become [TOKEN]
    - serialize_template produces natural language prompt from template + data:
      - Starts with "Create a {subject}" from core.subject
      - Includes populated objects from core.objects with data substituted
      - Includes notes from core.notes with data substituted
      - Includes constraints from core.constraints
      - Includes style cues (primary_style, render_quality)
      - Includes hex color codes from extras.color_mapping (e.g., "Use #2E7D32 for approve")
      - Includes quality keywords (include items)
      - Includes extras.data fields with data substituted
    - serialize_template with style_override string appends it as "Additional style guidance:" section
    - Prompt does NOT contain raw JSON -- only natural language paragraphs
  </behavior>
  <action>
    RED phase: Create tests/test_generate_infographic.py with TestPromptSerialization class covering all behaviors above. Add conftest.py fixtures: sample_template (dict matching real template structure with 2-3 objects and color_mapping), sample_data (dict with placeholder values like DOMAIN_RECOMMENDATIONS="Finance: Approve (High)"), sample_template_path (fixture that writes sample template JSON to a temp directory).

    GREEN phase: Create scripts/generate_infographic.py with:
    - TEMPLATE_DIR = Path("templates/infographic-prompts")
    - PLACEHOLDER_RE = re.compile(r"\{\{(\w+)\}\}")
    - load_template(infographic_type: str) -> dict -- normalize slug, load JSON, raise ConfigError on missing
    - substitute_placeholders(text: str, data: dict) -> str -- regex replace {{TOKEN}} with data values
    - serialize_template(template: dict, data: dict, style_override: str | None = None) -> str -- flatten JSON to natural language prompt following the structure in the behavior spec

    Per user decision: flatten to natural language paragraphs, not keyword lists. Include hex colors from extras.color_mapping. Per research: descriptive paragraphs produce better Gemini images than keyword lists.

    Also add save_prompt(prompt: str, output_dir: Path, type_slug: str) -> Path that writes prompt to {output_dir}/INFOGRAPHIC_{type-slug}_PROMPT.txt (per user decision: always save assembled prompt alongside PNG).

    REFACTOR if needed: ensure clean separation between load/substitute/serialize.
  </action>
  <verify>
    <automated>python -m pytest tests/test_generate_infographic.py::TestPromptSerialization -x -v</automated>
  </verify>
  <done>
    - load_template reads JSON templates by normalized slug and raises ConfigError on missing
    - substitute_placeholders replaces all {{TOKEN}} patterns with data values
    - serialize_template produces natural language prompt with subject, objects, notes, constraints, style, colors, and quality cues
    - style_override appends when provided
    - All tests pass
  </done>
</task>

</tasks>

<verification>
python -m pytest tests/test_generate_infographic.py -x -v
</verification>

<success_criteria>
- Template loading works for all 6 infographic type slugs (hyphen and underscore variants)
- Placeholder substitution handles present keys, missing keys (fallback to [TOKEN]), and nested template text
- Serialized prompt is human-readable natural language paragraphs with hex color codes
- Prompt save writes to correct path with correct filename pattern
- All TestPromptSerialization tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-integration/02-01-SUMMARY.md`
</output>
